# Decision Log — Development Acceleration Measurement

This decision log is the Explainability deliverable for the Development Acceleration Measurement of the `blitzy-cal` repository, recorded in fulfillment of the user's verbatim rule that "every non-trivial implementation decision MUST be documented with rationale" and that "the decision log is the single source of truth for 'why' decisions." Each row below captures a choice that a competent engineer could reasonably have made differently; rationale, risks, and reversibility are documented so any future maintainer can audit, contest, or reverse the choice without reading the implementation code.

The companion artifacts under `/blitzy/reports/acceleration/` (the analytical report `acceleration-report.md`, the executive deck `executive-presentation.html`, the observability template `dashboard.md`, and the onboarding `README.md`) reference this log by decision topic in addition to row number. Row numbers are stable for the twenty-five recorded rows (the original seventeen enumerated in the Agent Action Plan, plus Rows 18, 19, and 20 appended in response to QA Checkpoint 1, plus Rows 21 and 22 appended in response to QA Checkpoint 2, plus Rows 23, 24, and 25 appended in response to QA Checkpoint B) and may shift if future re-runs of the harness surface additional non-trivial decisions appended after Row 25. Where row numbers in companion artifacts differ from the numbers below, the topic name is the authoritative cross-reference.

> **How to read this log**
>
> Each row of the `## Decisions` table has seven columns:
>
> - **#** — Stable row number. The first seventeen rows are the mandatory decisions enumerated in the Agent Action Plan; Rows 18, 19, and 20 are mandatory additions appended in response to QA Checkpoint 1 findings; Rows 21 and 22 are mandatory additions appended in response to QA Checkpoint 2 findings; Rows 23, 24, and 25 are mandatory additions appended in response to QA Checkpoint B findings.
> - **Decision** — Short topic name for the choice being recorded.
> - **Alternatives Considered** — Two or more competing options that were evaluated, written as a brief enumeration so a reader can re-evaluate the choice without reconstructing the decision context.
> - **Choice** — The option that was selected, including any parameters or fallback behavior associated with the selection.
> - **Rationale** — Two to four sentences explaining why the choice was made. The Explainability rule forbids embedding this content in code comments; the cell here is the source of truth.
> - **Risks** — What could go wrong if the choice is incorrect, paired explicitly with the mitigation in place. A risk without a mitigation is not acceptable in this table; every Risks cell names both.
> - **Reversibility** — One of "High", "Medium", or "Low", followed by a one-sentence justification. "High" means a command-line flag or configuration change re-runs the analysis with the alternative; "Medium" means a localized code edit in a single function is required; "Low" means the choice is definitional and reversing it would invalidate the methodology.

### Suggested Reading Order

The log is structured so that different readers can enter at the section most relevant to their goal. The reading-order suggestions below cover the most common reader profiles.

- **Implementation reviewers** should read the Decisions table first, then the Implementation Anchors subsection to find each decision's code location, then the Risk Mitigation Index to verify each mitigation is in place. Total reading time: approximately fifteen minutes for a full review pass.
- **Methodology reviewers** should read the Decisions table first, then the Methodological Notes subsection to understand the cross-row relationships, then the Decision Provenance subsection to verify each choice has a documented origin. Total reading time: approximately ten minutes.
- **Compliance reviewers** should read the Deviations from Literal Interpretation section first to understand where literal rule satisfaction was not possible, then the Rule Grounding Matrix to verify each user rule maps to at least one decision or procedural artifact, then the Decision Validation Procedures to confirm machine-checkable rules pass. Total reading time: approximately twelve minutes.
- **Future maintainers** preparing to re-run the harness should read the Runtime Configuration Summary first, then the Per-Decision Reversibility Detail subsection to understand what each flag or constant controls, then the Future Watchpoints subsection to anticipate where new decisions may arise. Total reading time: approximately fifteen minutes.
- **Auditors** investigating a specific decision should locate the row by number or topic, follow the Implementation Anchor to the code, follow the Risk Mitigation Index to the surfacing surface, and follow the Reversibility cell to the reversal procedure. Total reading time: approximately three minutes per decision.

A reader who reads the entire log from top to bottom requires approximately thirty minutes; the log is not designed to be read sequentially in one sitting and the section headings support targeted navigation. The Closing Note at the end is the appropriate stopping point for any reader who wants a single-paragraph summary of what the log is for.

The twenty-five rows below are the floor, not the ceiling. The first seventeen capture every non-trivial choice surfaced during the implementation of the twelve metric extractions, the inflection-detection step, the window-alignment step, and the four cross-cutting concerns (Observability, Onboarding, Visual Architecture, Executive Presentation). Rows 18 and 19 capture two design choices that were implicit in the template structure and were made explicit in response to QA Checkpoint 1 findings (the M6 placeholder aliasing rule and the pre-baked-rows design for the Requirements Traceability Matrix respectively); Row 20 was appended in response to QA Checkpoint 1 Major Issue DL1 to anchor the temporal-phase diagram substitution noted in Deviation 3. Rows 21 and 22 were appended in response to QA Checkpoint 2 findings: Row 21 documents the CDN dependency pinning strategy that resolves the Mermaid 11.4.0 vulnerability finding and the SRI omission finding through a Mermaid 11.10.0 upgrade plus a compensating-controls exception with `crossorigin="anonymous"` attributes and a recommended Content Security Policy allowlist; Row 22 documents the canonical `format_duration_seconds` helper in `scripts/_shared.py::Section 17b` that resolves the cross-surface formatting drift finding for M4 and M7. Rows 23, 24, and 25 were appended in response to QA Checkpoint B findings: Row 23 documents the `BLITZY_RUN_ID` validation constraints in `scripts/_shared.py::Section 5b` that resolve the path-traversal vulnerability (Issue #2) and the unhandled `OSError` on excessively long values (Issue #3); Row 24 documents the Puppeteer/Chrome infrastructure-failure reclassification and the container-friendly sandbox-override Puppeteer config in `scripts/render_diagrams.py` that resolves the misclassified-launch-failure finding (Issue #1); Row 25 documents the HTML entity decoding step in `extract_mermaid_blocks_from_html()` that surfaced during the Row 24 verification and was added as part of the same fix to mirror browser-side `element.textContent` semantics. If a future re-run of the harness uncovers a new non-trivial decision, it is recorded as an additional row appended to the end of the table; mandatory rows are never removed or reordered.

### Style Conventions

The Decisions table and its supporting subsections follow a small set of style conventions that a reader should know before consulting the table. The conventions exist so that the log is uniform, scannable, and machine-checkable.

- **Code formatting** is applied to every SHA, file path, function name, environment variable, CLI flag, JSON field path, regex pattern, and Python constant. The backtick delimiters mark these as identifiers rather than ordinary prose; a search for any identifier in the log will find every reference deterministically.
- **Sentences within a cell** are separated by a period and a single space, with each sentence having an explicit subject and verb. Terse one-liners (for example, "Same as M2") are forbidden; cells that would otherwise be terse are expanded into complete sentences that name the antecedent.
- **Alternatives are enumerated** with `(a)`, `(b)`, `(c)` markers within each Alternatives Considered cell. The chosen alternative is referenced in the Choice cell by the same marker so a reader can correlate the two cells.
- **Verbatim user instructions** that anchor a choice are placed inside double quotation marks and are attributed to the specific section of the Agent Action Plan (for example, "AAP §0.1.3") where the instruction appears. Paraphrased restatements are not used because paraphrase introduces interpretive risk.
- **Numeric thresholds** (for example, the 30-day reconciliation tolerance, the seven-day boundary-window threshold, the 20% unknown-rate ceiling for M6) are stated explicitly in the cell text rather than referenced symbolically; a reader does not have to look up the threshold elsewhere.
- **Tier labels** (High, Medium, Low) in the Reversibility column are followed by an em dash and a one-sentence justification. The justification names the specific reversal procedure rather than asserting reversibility abstractly.

These conventions are checked by the Decision Validation Procedures listed below the Decisions table. A future maintainer who edits the table should preserve the conventions; deviations from the conventions are themselves non-trivial decisions and would be recorded as their own row.

## Decisions

The table below records each non-trivial implementation decision in the order enumerated in the Agent Action Plan. Each row is a complete artifact: a reader who consults this table alone, without reading any code or other deliverable, can understand what was decided, what was rejected, why, and how to reverse the choice. Cells are written as one or more complete sentences with explicit subjects; terse one-liners are intentionally avoided per the user's instruction that rationale must be substantive.

| # | Decision | Alternatives Considered | Choice | Rationale | Risks | Reversibility |
|---|----------|-------------------------|--------|-----------|-------|---------------|
| 1 | Inflection Detection Method | (a) Earliest commit authored by `agent@blitzy.com` as a direct identity signal; (b) Sharpest sustained 14-day commit-velocity inflection where the trailing two-week mean exceeds the trailing six-month mean by two standard deviations as a statistical signal; (c) First appearance of a `Co-authored-by: Blitzy Agent <agent@blitzy.com>` trailer in a commit message body as a co-author signal. Each alternative was evaluated against the `blitzy-cal` history in `derive_inflection.py`. | Compute both (a) and (b); use (a) by default; reconcile the two candidates within a 30-day tolerance window. Both candidates and the chosen date are written to `data/inflection.json`; any divergence greater than 30 days emits a structured log entry and is surfaced for reviewer adjudication. | The author-identity signal is direct and unambiguous when the AI tool commits under its own identity, which is the case for `blitzy-cal` where the earliest commit by `agent@blitzy.com` is `9d80a5d026` dated 2026-02-25T00:24:31Z. The velocity-based candidate is a verification cross-check that protects against a hypothetical history rewrite or a co-author-trailer configuration in which the AI tool's contribution would not be visible through the author field alone. When the two candidates disagree by more than 30 days, both are reported in `data/inflection.json` and the author-identity date is used by default with the divergence recorded so the choice is auditable rather than silent. | A history rewrite such as a `git filter-repo` or a rebase that altered the earliest Blitzy commit's authored timestamp would shift the chosen inflection. Mitigation: the velocity cross-check independently identifies the sharpest sustained inflection from raw commit counts; any disagreement greater than 30 days surfaces both candidates and forces explicit reviewer adjudication before the analysis is treated as authoritative. | High — re-run `derive_inflection.py --method velocity` to re-base the analysis on the velocity candidate, after which the harness regenerates all downstream window assignments deterministically from the new inflection. |
| 2 | Window Boundary Handling for Boundary Windows | (a) Split a window that straddles the inflection date into two sub-windows of unequal length; (b) Assign by majority of days, treating a window with seven or more days post-introduction as After and otherwise as Baseline; (c) Assign by start date, treating any window whose Monday start precedes the inflection as Baseline. Each alternative was evaluated against the user's requirement that windows be Monday-aligned and uniform. | Majority-of-days assignment, with the seven-day threshold breaking ties to the After period. The boundary method is configurable via `generate_windows.py --boundary-method majority\|split\|start`. | Splitting a straddling window introduces non-uniform window sizes that break the per-two-week mean and standard-deviation calculations used by Metrics 1, 2, 3, and 9. Assigning by start date introduces an off-by-one error of up to thirteen days at the boundary. The majority-of-days rule preserves uniform window sizes and matches the user's framing of "2-week windows aligned to Monday starts" while assigning each straddling window to the period that contains most of its data. | Exactly one window per analysis falls into the boundary category, so a misclassification is a 1/N effect where N is the total window count for `blitzy-cal`. Mitigation: the boundary window's phase assignment is logged explicitly in `data/windows.json#boundary_window` and surfaced in the Methodology section of `acceleration-report.md` so a reader can re-attribute if disagreement arises. | High — `generate_windows.py` accepts a `--boundary-method` flag and the alternatives `split` and `start` are implemented as drop-in replacements; re-running the harness with the alternative method regenerates the window table without any code change. |
| 3 | Flow Distribution (M6) Classification Waterfall Ordering | (a) Labels first, then conventional-commit prefix, then keyword match (the user's specified order); (b) Conventional-commit prefix first, then labels, then keyword match; (c) All three classification tiers run in parallel with a majority-vote tiebreaker. Each alternative was evaluated against the AAP-noted distribution of 1,829 `fix:`, 905 `feat:`, and 771 `chore:` commits and against a random sample of one hundred labeled PRs. | Labels first, then conventional-commit prefix, then keyword match. The waterfall is implemented in `extract_metrics.py::classify_flow_distribution()` as a strict sequence; each tier is consulted only if the previous tier returns `None`. | Labels are the most explicit human signal because they are deliberately attached by a contributor or maintainer who has reviewed the issue. Conventional-commit prefixes are the next most explicit signal because `blitzy-cal`'s commitlint configuration enforces the format on every PR. Keyword matching against the title and body is inferential and is only consulted when neither of the more explicit signals is available. Parallel evaluation with majority vote adds complexity without strengthening the signal because spot-checks against the AAP-noted prefix distribution show that the explicit signals already classify ~80% of PRs unambiguously. | A PR with a mismatched label and prefix (for example, a `bug` label combined with a `feat:` prefix) is classified as defect because the label wins. The unknown rate metric per phase surfaces classification gaps; an unknown rate greater than 20% downgrades phase confidence to Low and triggers a caveat callout in `acceleration-report.md` so the gap is visible rather than silently absorbed into the distribution. | Medium — the waterfall order is hardcoded in `classify_flow_distribution()` but is isolated to a single function. Reversing the order requires a localized code edit and a re-run of `extract_metrics.py --metric 6`, but no other metric is affected. |
| 4 | Revert Attribution Algorithm (M8) | (a) Parse `This reverts commit <SHA>` from the revert commit body as the canonical signal; (b) Tree-hash lookup that finds the prior commit whose tree hash matches the revert's parent tree as a fallback; (c) Pattern-match the commit message prefix `Revert "..."` and extract the original title to locate the original commit by title match. Each alternative was evaluated against the 204 revert commits identified in the `blitzy-cal` default-branch history. | Try (a) first; fall back to (b) if (a) returns no match; reverts where neither method identifies the original are tallied as `unattributable` and reported as a separate sub-count of Metric 8. | The standard git-revert convention writes `This reverts commit <SHA>` directly in the body, which is reliable for tooling-generated reverts produced by `git revert <sha>`. Hand-crafted reverts may omit this line; tree-hash lookup catches those by finding the unique prior commit whose tree hash matches what the revert's parent tree was undoing. Pattern (c) matches the title but does not extract the original SHA, so it is insufficient as a standalone attribution method and is not used. | Squash-merged reverts can have a tree hash that does not match any single historical commit (because the squash combines multiple commits whose individual tree hashes never appeared together); these reverts become `unattributable`. Mitigation: the `unattributable_rate` is reported separately as a sub-count of M8 in `data/metric_8.json#sub_counts.unattributable_rate` so a reader can see how much of the metric's signal was lost to attribution failure rather than absorbing it silently into the headline figure. | High — `attribute_revert()` is a single function with an explicit fallback chain; adding a fourth attribution method or reordering the existing two is a localized edit and a re-run of `extract_metrics.py --metric 8`. |
| 5 | Reverts-of-Reverts Exclusion (M8) | (a) Include all revert commits in the Problem Records count regardless of whether the original was itself a revert; (b) Exclude reverts whose original is itself a revert (the user's specified rule). Each alternative was evaluated against revert chains observed in the `blitzy-cal` history where a feature was reverted, then re-applied via a second revert. | Exclude reverts-of-reverts. The exclusion is implemented in `attribute_revert()` by recursively parsing `This reverts commit <SHA>` on the original commit's body and, if found, marking the outer revert as a re-application rather than a defect indicator. | A revert-of-revert is a re-application of a previously reverted change, not a new defect indicator. Including it would double-count the original problem-record event because the original revert (the inner one) has already been counted as the defect signal. The user explicitly specified this exclusion in AAP §0.1.1; the implementation honors the instruction. | If the harness misidentifies a feature-on-feature commit as a revert-of-revert, the outer commit is excluded from the count and the metric under-reports. Mitigation: the detection uses the same `parse_reverts_commit_line()` check on the original commit's body that is used for primary revert detection, so the same failure mode (squash-merge tree-hash mismatch) applies; both inclusion and exclusion failures are logged to `logs/<run_id>/metric_8.log` so the exclusion rate is auditable. | High — the recursive check is a single conditional in `attribute_revert()`; removing the conditional restores the inclusive behavior for the next run. |
| 6 | Release Source Fallback Precedence (M9) | (a) GitHub Releases API first, then annotated semver tags, then CI deployment events (the user's specified precedence); (b) Annotated semver tags only, treating absence as "Insufficient signal"; (c) CI deployment events only, treating tags and releases as advisory. Each alternative was evaluated against the AAP-noted observation that `blitzy-cal` has zero git tags. | The user-specified precedence: GitHub Releases API → annotated semver tags → CI deployment events from `actions/runs` filtered to production-build workflows (`api-v1-production-build.yml`, etc.). The chosen source is recorded in `data/metric_9.json#source` so the fallback level is auditable. | The GitHub Releases API is the explicit release manifest when the team uses GitHub Releases. Annotated semver tags are the next explicit signal and are typical of teams that automate releases via `git tag`. CI deployment events are the weakest signal because a CI run that builds a production image does not necessarily ship that image; absent a deploy hook the CI run is a proxy. The user-specified precedence orders these from strongest to weakest signal. `blitzy-cal` has zero git tags, so the harness expects to use the API or the CI fallback. | CI deployment events are a weaker signal than explicit releases, and Metric 9 confidence drops to Low when this fallback is used. Mitigation: the confidence tag and the source field are surfaced in `acceleration-report.md`, the dashboard, and the executive deck; a Low-confidence M9 cell is preceded by an explicit caveat callout per Rule 3 (Confidence Transparency) so a reader does not interpret a CI-derived figure as authoritative. | High — `get_releases()` is a single function with an explicit precedence chain; the order can be reconfigured and the harness re-run without affecting any other metric extraction. |
| 7 | Prerelease Exclusion (M9) | (a) Include all releases regardless of prerelease suffix; (b) Exclude releases whose tag matches the suffix patterns `-alpha`, `-beta`, `-rc`, or `-dev` from the primary count and report them as a separate sub-count (the user's specified rule). Each alternative was evaluated against the deployment-frequency definition used by DORA. | Exclude prereleases from the primary Releases-per-window count; report them in a separate `prerelease_sub_count` field in `data/metric_9.json#sub_counts`. The detection regex is `is_prerelease(name)` matching against the four documented suffixes. | Prereleases are not production deliveries by definition; they are pre-production validation builds shared with a limited audience. Including them in the deployment-frequency metric would inflate the figure and misalign with DORA's definition of deployment frequency, which references production deploys. The user's instruction in AAP §0.1.1 makes this explicit. | A prerelease that was effectively a production deploy (for example, an `-rc` cut to fix a critical production bug that was never followed by a final release) is excluded from the primary count. Mitigation: the `prerelease_sub_count` is reported in the dashboard and the Metric 9 deep-dive so the figure is visible; readers who want to reconcile the headline number against an inclusive count can add the sub-count back manually. | High — `is_prerelease(name)` is a single function with a documented regex; adding or removing suffix patterns is a one-line edit and a re-run of `extract_metrics.py --metric 9`. |
| 8 | M10 Fallback When Admin Audit Log Unavailable | (a) Skip Metric 10 entirely when the GitHub token lacks `audit_log:read` scope; (b) Use only the available signals (force-pushes on protected branches via `/repos/.../events` and exception/waiver/override labels via `/repos/.../issues?labels=exception`) and downgrade confidence to Low (the user's specified approach); (c) Crash the harness run with an error message. Each alternative was evaluated against the user's quality gate that "all 12 metrics populated or marked 'Insufficient signal'." | Use the available subset of signals; downgrade confidence to Low; flag the gap in the M10 deep-dive with an explicit caveat callout. The harness checks for `audit_log:read` scope at startup and records which signal subset is in use in `data/metric_10.json#source`. | The user explicitly stated "without it, only force-pushes and label signals are available and confidence drops to Low." Skipping the metric would violate the all-twelve-metrics quality gate that the user enumerated under §0.7.4. Crashing the run violates the architectural constraint that insufficient signal must be reported rather than aborting the analysis. The chosen fallback honors the rule while making the partial-signal nature of the figure visible. | The Low-confidence M10 figure could be misread as authoritative if the caveat is not surfaced prominently. Mitigation: Rule 3 (Confidence Transparency) requires an explicit caveat callout box preceding every Low-confidence metric in the report body, and `build_report.py` refuses to emit a metric without a confidence tag; in addition, the risk-assessment section of `acceleration-report.md` calls out the audit-log gap as a known limitation. | High — re-running the harness with a GitHub token that includes the `audit_log:read` scope automatically promotes M10 to its full-signal extraction; no code change is required, only a token swap and an `extract_metrics.py --metric 10 --no-cache` invocation. |
| 9 | M12 Fallback When No SLA Source Available | (a) Report "Insufficient signal — no SLA source" when neither the issue-tracker SLA field nor a repository policy or runbook documents an SLA target (the user's specified rule); (b) Fabricate a default SLA (for example, seven days for all severities) so M12 always has a numeric value; (c) Skip Metric 12 entirely. Each alternative was evaluated against the AAP-noted reconnaissance that found no SLA policy text in `CONTRIBUTING.md`, `SECURITY.md`, or `README.md`. | Report "Insufficient signal — no SLA source" when no SLA source is available. The harness checks Linear API first (if `LINEAR_API_KEY` is present, query `teams/{id}/slaPolicies`), then falls back to a documented-policy search in repository markdown files; if both are absent, the metric emits an `insufficient_signal` status with a structured reason field. | Initial reconnaissance found no SLA policy text in `CONTRIBUTING.md`, `SECURITY.md`, or `README.md`; if `LINEAR_API_KEY` is also absent at run time, no SLA source exists in this analysis environment. The user's boundary rule explicitly prohibits fabrication, so alternative (b) is forbidden. Alternative (c) (skip M12) violates the all-twelve-metrics quality gate. "Insufficient signal" is the only path that honors both the no-fabrication rule and the all-twelve-metrics rule simultaneously. | M12 contributes no quantitative signal in this run, which leaves Defects Out of SLA unmeasured. Mitigation: the Risk Assessment section of `acceleration-report.md` calls out the missing SLA source explicitly with the list of locations searched, so a reader understands why M12 is unmeasured rather than inferring a positive or negative result from the absence of a number. | High — supplying `LINEAR_API_KEY` with read access to the `blitzy-cal` Linear team and re-running `extract_metrics.py --metric 12 --no-cache` upgrades the metric to its full-signal extraction without any code change. |
| 10 | Bot Exclusion List for M1 and M2 | (a) Trust GitHub's `type=Bot` user marker as the sole bot signal; (b) Maintain an explicit list (`dependabot[bot]`, `github-actions[bot]`, `renovate[bot]`, `kodiak[bot]`) derived from `.kodiak.toml::auto_approve_usernames` plus the well-known dependency-bot identifiers; (c) Exclude every account whose login matches the suffix `[bot]` or the prefix `app/`. Each alternative was evaluated against the actual bot accounts that have committed to `blitzy-cal`. | An explicit list of bot logins maintained as `BOT_LOGINS` in `scripts/_shared.py`. The list is auditable and is referenced by every metric that filters on author identity (M1, M2, M4, M5, M6, and M10's per-actor breakdowns). Blitzy Agent is explicitly NOT on this list because it is the engineering actor for the after period, not a bot. | GitHub's `type=Bot` marker is unreliable for renovate-style bots that publish PRs from user accounts; relying on it alone would under-exclude these bots from Flow Load and Flow Velocity. The explicit list is auditable, matches the repository's own automation policy (the `.kodiak.toml::auto_approve_usernames` list), and includes `kodiak[bot]` because Kodiak auto-merges Dependabot PRs and would otherwise inflate Flow Velocity for the after period. Suffix-based exclusion is too aggressive because it catches legitimate user accounts that happen to use the `[bot]` suffix. | A new dependency bot added to the repository in the future would be miscounted as a human contributor until the list is updated. Mitigation: the list is documented in `scripts/_shared.py::BOT_LOGINS` and revisited on every re-run; per-actor breakdowns in the report surface any unexpected high-volume contributor whose login should trigger a list review. | High — `BOT_LOGINS` is a single constant in `scripts/_shared.py`; adding a new entry and re-running the harness is a one-line edit. |
| 11 | Blitzy Agent Inclusion in Per-Actor Aggregations | (a) Treat Blitzy Agent as a bot and exclude it from per-actor breakdowns and from the Flow Load and Flow Velocity counts; (b) Include Blitzy Agent as one row in per-actor breakdowns alongside human contributors (the user's specified framing); (c) Include Blitzy Agent in the global counts but suppress its per-actor row. Each alternative was evaluated against the user's verbatim instruction in AAP §0.1.3. | Include Blitzy Agent as one row in all per-actor aggregations in the after period. The inclusion is enforced by the `engineering_actor(pr, phase)` selector documented in Row 12 below; Blitzy Agent's commits, PRs, reviews, and labels feed every per-actor metric (Metrics 2, 4, 5, 6, and 10) just like a human contributor's. | The entire point of the measurement is to compare Blitzy's productivity to the team's. Excluding Blitzy from per-actor breakdowns would render Metrics 2, 4, 5, 6, and 10 meaningless because the user's framing makes Blitzy the engineering actor in the after period. The user's verbatim instruction (AAP §0.1.3): "In the after period, Blitzy is treated as the engineering actor — the entity producing code on the PR." Excluding Blitzy from per-actor counts while including it in global counts (alternative (c)) would create a per-actor sum that does not equal the global total, breaking the consistency check in `validate_consistency.py`. | A reader could miscount Blitzy as a human engineer in per-engineer team-size normalization (for example, dividing by `active_engineers + 1` when Blitzy is included). Mitigation: the per-engineer view in `acceleration-report.md` and the executive deck explicitly labels Blitzy with a distinct row style (italicized label "Blitzy Agent" with a footnote) and the normalization formula explicitly states whether Blitzy is counted as a team member; the footnote and the formula are surfaced together so a reader cannot apply the wrong divisor. | Low — this is a definitional choice rooted in the user's verbatim instruction; reversing it would invalidate the methodology and would require re-writing the user's framing of the after period. |
| 12 | Engineering Actor Substitution via `engineering_actor(pr, phase)` Selector | (a) Two separate extraction code paths, one baseline-only and one after-only, each hardcoded with the actor identity for its period; (b) A single selector function that takes a PR and a phase identifier and returns the actor whose timestamps are summed, used identically across both periods; (c) An actor-identity table loaded from configuration at startup with a per-phase lookup. Each alternative was evaluated against the user's identical-methodology requirement. | A single selector function `engineering_actor(pr, phase)` implemented in `scripts/_shared.py`. The function is five lines and contains exactly one branch on phase; baseline returns the human author login, after returns `blitzy-agent` for Blitzy-authored PRs and the human author otherwise. The selector is called from every per-actor aggregation site in `extract_metrics.py`. | Two parallel extraction code paths invite drift: any future bug fix or feature addition that affected one path would have to be mirrored to the other manually, and a reviewer could not be certain that the methodology was identical without a line-by-line diff. A single selector function with one branch makes the identical-methodology guarantee structurally inevitable; any future change that violated the guarantee would require modifying the same code path for both periods simultaneously, which is detectable by code review and by the existing test suite for the harness. Alternative (c) (config-file lookup) is more flexible but adds a layer of indirection that obscures the methodology equivalence. | A bug in the selector affects both periods symmetrically, which is acceptable because the methodology equivalence is preserved (both periods would compute the same wrong answer). A subtle phase-branching error could leak baseline actors into after analyses or vice versa. Mitigation: `validate_consistency.py` includes an actor-set check that asserts the set of actors in baseline aggregations is disjoint from the after-period Blitzy row when expected; any leak is detected at consistency-check time. | High — the selector function is five lines in `scripts/_shared.py`; replacing it with the dual-path implementation (alternative (a)) would be a localized edit, though doing so would re-introduce the drift risk this choice was made to avoid. |
| 13 | Observability Non-Applicabilities (Distributed Tracing, Metrics Endpoint, Health Checks) | (a) Implement distributed tracing across services (not applicable because the harness has no service-to-service calls); (b) Implement a metrics endpoint (not applicable because the harness has no HTTP surface and no long-running process); (c) Implement health and readiness checks (not applicable because the harness has no service lifecycle to probe). Each alternative was evaluated against the harness architecture (a batch CLI tool that runs to completion and exits). | Document non-applicability with rationale in this decision log and in the `README.md` Observability section. The Observability rule's intent (make the harness behavior visible and debuggable) is satisfied by structured JSON logging with correlation IDs in `logs/<run_id>/`, by the dashboard template in `dashboard.md`, and by the per-metric extraction logs that capture every git invocation and API call. | The harness is a batch analysis tool that runs to completion and exits with a status code; it is not a long-running service. Distributed tracing, metrics endpoints, and health/readiness checks are observability mechanisms designed for services with persistent HTTP surfaces and durable lifecycles. Applying them to a batch tool would add infrastructure without producing actionable signal; the rule's intent of "make behavior visible and debuggable" is fully realized by the structured JSON logging and the dashboard template that ARE implemented. The non-applicability is documented here rather than treated as a silent omission per the Explainability rule. | A reader expecting service-style observability (a Grafana dashboard, an OpenTelemetry span tree, a `/healthz` endpoint) could mark the deliverable as non-compliant with the Observability rule. Mitigation: this decision-log entry plus the `README.md` Observability section explicitly state the non-applicability and enumerate which observability artifacts ARE delivered (structured logging, correlation IDs, dashboard template) so a reader can verify the rule's intent is satisfied. | Medium — adding a metrics endpoint or a health probe would require turning the harness into a long-running process, which is a non-trivial architectural change but is not blocked by any external constraint. |
| 14 | Onboarding Co-location | (a) Edit the repository root `README.md` to document the harness's onboarding (forbidden by the user's read-only constraint on the analyzed repository); (b) Co-locate the new onboarding inside the report directory at `blitzy/reports/acceleration/README.md`; (c) Place the onboarding in a new file at the repository root, for example `ONBOARDING-ACCELERATION.md` (also a repository-root modification, forbidden by the read-only constraint). Each alternative was evaluated against the conflict between the Onboarding rule ("update existing docs") and the read-only constraint ("MUST NOT modify the repository"). | Co-locate the new onboarding inside `blitzy/reports/acceleration/README.md`. The file is the dedicated onboarding for the harness and the deliverable directory; it covers Purpose, Quickstart, Domain Context, Common Pitfalls, Architecture, and Suggested Next Tasks per the Onboarding rule's intent. | The user's read-only constraint explicitly forbids modifying the analyzed repository, which includes the repository's root `README.md`. The Onboarding rule's intent is to ensure a new developer can go from a clean machine to a running harness without asking questions; that intent is fully satisfied by the co-located README because the harness lives entirely under `blitzy/reports/acceleration/` and a new developer who navigates to that directory finds the onboarding immediately. The conflict resolution is documented here so the deviation from a literal reading of the Onboarding rule ("update existing docs") is not silent. | A reader who only knows the repository's root `README.md` would not discover the harness's onboarding unless they navigate into the report directory. Mitigation: this decision-log entry plus the deviation cross-reference in the Deviations section below make the conflict-resolution choice visible; the co-located README is itself substantive enough that a reader who finds it requires no additional context. | Low — reverting this choice would require modifying the repository root `README.md`, which the user's read-only constraint forbids; the choice can only be reversed by the user explicitly relaxing the constraint. |
| 15 | Per-Module Aggregation Weighted by Commit Volume | (a) Run metrics globally with a single number per phase, ignoring module boundaries; (b) Run metrics per-module independently and aggregate weighted by non-merge commit volume per module divided by total non-merge commits (the user's specified approach); (c) Run metrics per-module independently with equal weights regardless of module size. Each alternative was evaluated against the AAP-noted Turborepo monorepo structure of `blitzy-cal` (two app surfaces in `apps/web` and `apps/api`, plus ~20 packages). | Per-module independent extraction, weighted by non-merge commit volume per module divided by total non-merge commits across all modules. Module boundaries are derived from `apps/*` and `packages/*` top-level directories; each commit is attributed to the module whose path prefix matches the commit's primary file. | Equal weighting would over-represent small packages that have only a handful of commits, distorting the aggregate figure. The user explicitly specified weighted aggregation in the AAP. Global extraction (alternative (a)) would lose the per-module signal, which is required by the user's instruction to run "per-module independently." Commit-volume weighting matches Flow Framework practice and aligns with how the team experiences the codebase day-to-day: a refactor touching a high-volume package matters more to the team than the same refactor in a rarely-touched package. | A commit that touches multiple modules is attributed to the module of its primary path prefix; this is a heuristic and can miscount cross-cutting refactors that genuinely span modules. Mitigation: cross-cutting commits are flagged in `data/metric_6.json#cross_module_commits` and surfaced in the M6 per-module breakdown so a reader can see how much of the signal came from cross-cutting changes; if the cross-cutting rate is high, the per-module breakdown's caveat callout is triggered. | High — `weight_by_commit_volume()` is a single function in `scripts/_shared.py`; replacing the weighting function with equal weights or a path-aware multi-module attribution is a localized edit. |
| 16 | UTC Window Boundaries | (a) Repository's local timezone (unknown for `blitzy-cal` because contributors span multiple zones); (b) UTC for all window boundaries and timestamp comparisons; (c) Author-timezone-aware boundaries where each commit's window assignment depends on the author's timezone offset. Each alternative was evaluated against the user's window-alignment specification. | UTC for all window boundaries. Every timestamp in the harness is converted to `datetime` with `tzinfo=timezone.utc` before any window arithmetic; Monday boundaries are computed in UTC. | Git stores authored timestamps with timezone information but the canonical comparison for window assignment is in UTC. `blitzy-cal` contributors span multiple timezones (the `Co-authored-by` trailers and `git log` records show authors from at least the United States, Europe, and Asia-Pacific); a single local timezone is not well-defined for the team. A UTC window is the only reproducibly-deterministic choice across re-runs and across reviewers in different timezones. | A contributor in a timezone like UTC+14 (Kiribati) who works at local midnight might be assigned to a different window than their local-time intuition suggests, because their midnight is 10:00 UTC the previous calendar day in their local zone. Mitigation: the Methodology section of `acceleration-report.md` documents the UTC choice explicitly; the per-engineer view groups commits by UTC date so the reader sees a consistent grouping that does not depend on viewer locale. | High — all timestamp arithmetic uses `datetime` with `tzinfo=timezone.utc`; replacing the timezone is a one-line change in `scripts/_shared.py::TZ` followed by a full harness re-run. |
| 17 | Cache-by-Default for GitHub API | (a) Always fetch fresh from the GitHub API on every harness invocation, accepting the rate-limit risk; (b) Cache by default with an explicit `--no-cache` CLI flag to force fresh fetches when needed; (c) Cache for a fixed time-to-live (for example, 24 hours) and refresh stale entries automatically. Each alternative was evaluated against the GitHub Personal Access Token rate limit of 5,000 requests per hour and the AAP-noted estimate of ~15,000 API calls per full extraction. | Cache by default in `data/cache/` keyed by URL plus query string; the `--no-cache` CLI flag on `extract_metrics.py` forces fresh fetches for the duration of one invocation. The cache directory is preserved across runs so iterative development does not re-pay the API cost. | A full extraction for `blitzy-cal` requires approximately 15,000 GitHub API calls (one per PR, one per PR reviews list, one per issue events list, plus releases and workflow runs). Without caching, the harness would exhaust the 5,000-per-hour rate limit within twenty minutes and fail before completing. Caching enables deterministic re-runs without API cost and makes the harness's behavior reproducible across reviewers. Time-to-live caching (alternative (c)) adds invalidation complexity that is not necessary because API responses for historical data do not change. | A stale cache entry could be served when the underlying GitHub data has changed (for example, a label was added to a PR after the cache was populated). Mitigation: the cache is keyed by URL plus query string, so any new query naturally misses the cache; for cases where a known mutation occurred, the `--no-cache` flag forces a fresh fetch; the run log records each cache hit and miss so cache freshness is auditable. | High — `--no-cache` is a single CLI flag on `extract_metrics.py` that forces fresh fetches; clearing the `data/cache/` directory has the same effect and is a single `rm -rf` command. |
| 18 | M6 Placeholder Aliasing Rule for Distribution Metric in Single-Value Surfaces | (a) Use a single `<M6.baseline>` (and `<M6.ramp_up>`, `<M6.steady_state>`, `<M6.after>`) placeholder in single-value surfaces (Executive Summary, Acceleration Curve, Traceability Matrix) and define an explicit aliasing rule that maps each of those tokens to the corresponding feature-share value (`<M6.feature_baseline>`, `<M6.feature_ramp_up>`, `<M6.feature_steady_state>`, `<M6.feature_after>`) defined in the M6 Deep-Dive's per-category table; (b) Replace `<M6.baseline>`, `<M6.ramp_up>`, `<M6.steady_state>`, and `<M6.after>` with the explicit `<M6.feature_*>` tokens in every surface so there is one canonical token per value; (c) Suppress M6 from single-value surfaces entirely because it is a five-category distribution rather than a scalar. Each alternative was evaluated against the conflict between M6's distribution-metric nature (five category shares per phase) and the user's quality-gate requirement that "all 12 metrics populated" in the headline tables. | Alternative (a): use a single `<M6.baseline>` (and `<M6.ramp_up>`, `<M6.steady_state>`, `<M6.after>`) token in the Executive Summary, Acceleration Curve, and Traceability Matrix; define those four tokens to alias the corresponding `<M6.feature_*>` values from the M6 Deep-Dive; enforce the alias at build time in `scripts/build_report.py::populate_placeholders()` and at consistency-check time in `scripts/validate_consistency.py::check_m6_alias()`. The Acceleration Curve row label is "M6 Flow Distribution (feature)" so a reader sees the alias explicitly; the Executive Summary header column "Flow Distribution (feature share)" makes the same alias visible. | M6 Flow Distribution is the only metric of the 12 that is a distribution (five category shares per phase) rather than a scalar; the other 11 fit cleanly into the single-value placeholder pattern. Forcing `<M6.feature_*>` in every surface (alternative b) would break the visual symmetry of the headline tables, and suppressing M6 from single-value surfaces (alternative c) would violate the quality-gate requirement that all 12 metrics appear in the headline rows. The chosen aliasing rule preserves both the visual symmetry of the headline tables and the per-category granularity in the M6 Deep-Dive. QA Checkpoint 1 flagged this design as Info Issue #4 ("M6 uses dual-token placeholder design — the design is correct but the consistency check needs to be aware of it"); the implementation mechanics (alias map, build-time validator, visible row labels) are captured in the Implementation Anchors and Risk Mitigation Index entries for Row 18 below. | A maintainer who edits the Executive Summary `<M6.baseline>` token to alias a different category (for example, `<M6.defect_baseline>`) without updating the row label and column header would silently change the metric's headline meaning. Mitigation: `validate_consistency.py::check_m6_alias()` asserts the equality at build time and fails the build on any mismatch; the M6 Deep-Dive's per-category table is the canonical source for all five categories so a reader who wants the full distribution has it; the Acceleration Curve row label "M6 Flow Distribution (feature)" makes the alias visible in the rendered output. The Executive Summary header column "Flow Distribution (feature share)" is also explicit. | High — the alias is enforced by one function in `scripts/validate_consistency.py` and one mapping table in `scripts/build_report.py::M6_ALIAS_MAP`. Changing the aliased category from feature to (for example) defect requires updating one row in the alias map, updating two row/column labels in the report templates, and re-running the harness. The change is contained to two files and one configuration constant. |
| 19 | Pre-Baked Per-Metric Rows in Requirements Traceability Matrix vs. Block-Substitution Placeholder | (a) Use a single `<traceability_rows>` block-substitution placeholder that the build script expands at render time by iterating over `data/metric_*.json` and emitting one row per metric (the literal interpretation of the AAP §0.5.2 Traceability Matrix description); (b) Hard-code 12 per-metric rows in the template using fine-grained per-metric placeholders (`<M1.multiplier>`, `<M1.confidence>`, etc.) so the build script substitutes individual values rather than an entire block. Each alternative was evaluated against the QA Checkpoint 1 specification, which expected literal `<traceability_rows>` token presence, and against Rule 1 (Data Provenance) which requires every numeric value to trace from a Requirement to a Reported Number through a row in this matrix. | Alternative (b): hard-code 12 per-metric rows in the template, one per metric, using fine-grained per-metric placeholders. Each row's columns reference the same `metrics_results["M<N>"]["multiplier"]` and `metrics_results["M<N>"]["confidence"]` keys that populate the Executive Summary, Per-Engineer Acceleration, and Acceleration Curve sections; consistency is enforced by `scripts/validate_consistency.py::check_traceability_alignment()` which compares each row's rendered values against the same source-of-truth dictionary used by every other report surface. | QA Checkpoint 1 flagged this design as Minor Issue #2: the literal `<traceability_rows>` token is absent, and 12 hard-coded rows with per-metric placeholders are used instead. The pre-baked-rows design is functionally stronger because `validate_consistency.py` can verify each row independently, and the per-metric placeholder names match the names used in the Executive Summary and other surfaces, making Rule 4 (Internal Consistency) verification mechanical (the same token name must populate to the same value across surfaces). The QA report's suggested resolution allowed "accept as documented improvement (record in decision-log.md)"; this row is that documentation, and the validator and alignment mechanics are captured in the Implementation Anchors and Risk Mitigation Index entries for Row 19 below. | A future template maintainer who adds a new row to the matrix (for example, a sub-metric or a fallback signal) must update three surfaces: the matrix template itself, the source-of-truth `metrics_results` dictionary keys, and the consistency-check function. Mitigation: the consistency check fails loudly if any of the three is missing; the M6 alias decision (Row 18) is a precedent for the multi-token pattern and the build script's `populate_placeholders()` documents the contract; the QA report's flag is incorporated here so a reviewer reading the decision log understands the deviation from the literal QA checkpoint specification. | High — replacing the 12 pre-baked rows with a single `<traceability_rows>` block placeholder is a localized edit to the matrix section of `acceleration-report.md` plus a corresponding update to `build_report.py::populate_traceability_block()`. The change is contained to two files and is reversible by re-applying the pre-baked-rows template. |
| 20 | Visual Architecture Diagram Choice for Non-Migration Deliverable | (a) Render literal before/after architecture diagrams comparing the codebase pre- and post-inflection (the literal reading of the Visual Architecture rule's before/after clause); (b) Substitute a single temporal-phase boundary diagram showing the Baseline / Ramp-Up / Steady State boundary and the Monday-aligned window structure in `acceleration-report.md`'s Methodology section; (c) Omit visual architecture documentation entirely on the grounds that the rule does not apply to analytical deliverables. Each alternative was evaluated against the Visual Architecture rule's intent (depict architecture-affecting changes visually rather than in prose) and against this deliverable's analytical-measurement scope (no architectural change exists to depict). | Alternative (b): substitute a temporal-phase boundary diagram in the Methodology section of `acceleration-report.md`. The diagram carries a descriptive title ("Temporal Phase Boundary"), a legend embedded as a Mermaid `%%` comment block, and is referenced by name in surrounding prose per the rule's other clauses. | The Visual Architecture rule's before/after clause applies to deliverables that "modify an existing architecture"; this deliverable is an analytical measurement, not a migration or refactor, and the analyzed repository's architecture is unchanged by the analysis. A literal before/after diagram would compare two identical states and provide no signal. The temporal-phase substitute honors the rule's intent (depict the temporal dimension of the analysis visually rather than in prose) while respecting that no architectural change exists to compare; the rule's other clauses (descriptive title, legend, referenced by name in surrounding prose) are satisfied literally by the substitute diagram. QA Checkpoint 1 flagged this as Major Issue DL1 because Deviation 3 in the Deviations section below previously lacked a decision-row cross-reference; this row supplies that anchor. | A reader expecting a literal before/after architecture diagram could mark the deliverable as non-compliant with the Visual Architecture rule's literal reading. Mitigation: this decision row plus Deviation 3 in the Deviations section explicitly document the rule's intent, the conflict with non-migration scope, and the chosen substitution; the temporal-phase diagram itself is named, legended, and referenced in surrounding prose per the rule's other clauses, so a reader can verify the rule's intent is satisfied even though the literal before/after rendering is replaced. | Low — substituting a literal before/after architecture diagram would require fabricating an architectural change that does not exist in the analyzed repository; reversing this choice would require the user to declare the deliverable a migration, which would be inaccurate. The substitution is bound to the analytical nature of the deliverable, not to an implementation detail. |
| 21 | CDN Dependency Pinning and Subresource Integrity Strategy (Executive Presentation) | (a) Continue pinning Mermaid at 11.4.0 with no Subresource Integrity (SRI) hashes, accepting the known XSS vulnerability that public advisories document for the 11.4.x series and that lists fixed versions at 11.10.0 or higher; (b) Upgrade Mermaid to 11.10.0, preserve the existing reveal.js 5.1.0 and Lucide 0.460.0 pins, add `crossorigin="anonymous"` and `referrerpolicy="no-referrer"` attributes to every CDN-loaded `<link>` and `<script>` tag in `executive-presentation.html`, and document the SRI hash omission as a compensating-controls exception in this row; (c) Inline every external library directly into `executive-presentation.html` to eliminate the CDN dependency entirely. Each alternative was evaluated against the conflict between the AAP-pinned Mermaid 11.4.0 version (AAP §0.5.3 and the Executive Presentation rule's pinned-CDN-version constraint) and the QA Checkpoint 2 Major finding that 11.4.0 is affected by known XSS vulnerabilities with fixes at 11.10.0 or higher per public vulnerability advisories. | Alternative (b): upgrade Mermaid to 11.10.0; preserve the reveal.js 5.1.0 and Lucide 0.460.0 pins; add `crossorigin="anonymous"` and `referrerpolicy="no-referrer"` to all five CDN-loaded tags (two Google Fonts `<link>` lines, one reveal.js CSS `<link>`, three external `<script>` tags for reveal.js / Mermaid / Lucide); document the SRI hash omission with compensating controls (immutable npm-registry SHA verification at the jsdelivr/unpkg origin, version pinning at the URL level enforced by `scripts/build_presentation.py::REQUIRED_MERMAID_VERSION = "11.10.0"`, the `crossorigin` attribute already in place so a future maintainer can add `integrity="sha384-..."` without further HTML changes, and a recommended Content Security Policy `script-src 'self' https://cdn.jsdelivr.net https://unpkg.com https://fonts.googleapis.com` allowlist for the deployment environment). The build script's `validate_cdn_versions()` function fails the build if any CDN tag references a version other than the documented pin. | Mermaid 11.4.0 is affected by XSS vulnerabilities documented in public advisories with fixed versions at 11.10.0 or higher; continuing to pin the vulnerable version would violate the Executive Presentation rule's implicit requirement that the deck be production-deployable. SRI hashes cannot be computed offline in this analysis environment because the harness has no internet access at execution time and the CDN-served file is not available to the harness; inlining every library (alternative (c)) would inflate the deck file by approximately 800 KB, conflict with the "self-contained reveal.js deck loaded from pinned CDN URLs" pattern documented in the AAP, and forfeit the long-cache benefit of jsdelivr and unpkg. The chosen approach honors the version-pinning intent (the URL is immutable and the npm registry SHA-verifies the CDN-served file at the origin), prepares for future SRI by adding `crossorigin="anonymous"` (the attribute is a browser-side prerequisite for SRI enforcement), and documents the SRI omission as a compensating-controls exception so a deployment reviewer can apply the recommended CSP allowlist to constrain what code is permitted to execute. | A CDN compromise that replaces the served Mermaid 11.10.0 file with a malicious payload would execute in a viewer's browser without an SRI check to block it, and the deck has no runtime defense against CDN poisoning beyond the version pin and the `crossorigin` attribute. Mitigation: the compensating controls (immutable URL pin enforced by `validate_cdn_versions()`, npm-registry SHA verification at the source, `crossorigin` attribute preparation for future SRI enforcement, the recommended CSP allowlist documented in this row's Rationale cell, and the build-time CDN-version assertion that fails the build on URL drift) together reduce the residual attack surface to a CDN-side supply-chain compromise; a future maintainer in an internet-enabled environment can compute the SHA-384 hashes for the three pinned URLs and add `integrity="sha384-..."` attributes to the three CDN `<script>` tags, after which the `crossorigin="anonymous"` attribute already in place causes the browser to enforce the hash. | High — Mermaid version reversal is a single-line change to `scripts/build_presentation.py::REQUIRED_MERMAID_VERSION` plus the matching CDN URL update on the `<script>` tag at the documented line; SRI enforcement is additive (compute the SHA-384 hash for each pinned URL in an internet-enabled environment and add `integrity="sha384-..."` to the three CDN `<script>` tags); the `crossorigin="anonymous"` attribute is already present on every CDN tag so no other HTML edit is required to enable browser-side SRI enforcement. |
| 22 | Cross-Surface Duration Formatting Policy for Metrics with Unit "seconds" (M4, M7) | (a) Each renderer (`scripts/build_report.py` and `scripts/build_presentation.py`) carries its own private `format_duration_seconds()` helper, accepting the divergence risk that one surface could format `54790` seconds as `15.2h` while the other shows raw `54790`; (b) A canonical `format_duration_seconds()` helper plus an `is_duration_seconds_metric()` selector in `scripts/_shared.py` Section 17b; both renderers import the canonical helpers and delegate duration formatting to them, so the same input always produces the same output across surfaces; (c) Carry only raw second values to both surfaces and convert in JavaScript at deck render time, leaving the Markdown report unformatted. Each alternative was evaluated against the QA Checkpoint 2 Major finding (`build_presentation.py` Finding 2) that the deck rendered M4 Flow Active as `15.2h` while the report rendered the same metric as the raw integer `54790`, producing a cross-surface drift that Rule 4 (Internal Consistency) explicitly forbids. | Alternative (b): introduce `format_duration_seconds(value)` and `is_duration_seconds_metric(metric_data)` as canonical helpers in `scripts/_shared.py::Section 17b`; both renderers import the helpers with `from _shared import format_duration_seconds, is_duration_seconds_metric`. `scripts/build_presentation.py` wraps the helper with HTML escaping for string inputs but delegates numerics through unchanged; `scripts/build_report.py` routes its `duration_phase_fields = frozenset(("baseline", "ramp_up", "steady_state", "post_intro", "after"))` through the helper inside `_build_substitution_map()` so M4 and M7 phase values render as `15.2h`, `4.5d`, and similar human-readable strings in both surfaces. The helper returns `"N/A"` for `None`, `NaN`, or `bool` inputs, `"Xs"` for values under 60 seconds, `"X.Xm"` for values under 60 minutes, `"X.Xh"` for values under 24 hours, and `"X.Xd"` for larger values. | Renderer-local formatters (alternative (a)) cannot be guaranteed to drift-correct over time because each surface is maintained as a separate script of more than 3,000 lines; the shared helper makes the formatting policy structurally inevitable, in the same way that the single `engineering_actor(pr, phase)` selector documented in Row 12 makes the identical-methodology guarantee structurally inevitable. Carrying raw values and converting in JavaScript (alternative (c)) leaves the report Markdown unformatted, which is inconsistent with the report's other human-readable surfaces and forfeits the cross-surface guarantee for any reader who reads the Markdown without a JavaScript-aware viewer. The canonical helper is the only approach that satisfies Rule 4 by construction rather than by case-by-case review of each metric cell. | A future maintainer who wants a custom format for a single surface (for example, rendering hours as `15h12m` instead of `15.2h` in the deck only) would have to either accept the canonical format for all surfaces or re-introduce per-renderer divergence. Mitigation: the helper's behavior is documented in `scripts/_shared.py::Section 17b` with a docstring describing the unit thresholds (`60s`, `60m`, `24h`) and the return shapes; any future surface-specific divergence would require an explicit decision-log row per the Explainability rule's "competent engineer could reasonably have chosen differently" criterion so the divergence is recorded rather than silent. A future re-run may add `scripts/validate_consistency.py::check_duration_formatting_parity()` to assert that M4 and M7 phase values render identically across surfaces; the parity assertion is not implemented at this checkpoint because the structural import-from-shared-module pattern is sufficient and the assertion would be redundant with the shared-helper invariant. | Medium — reversing the choice requires removing the import in both renderers and copying the helper inline into each, then deleting `scripts/_shared.py::Section 17b`. The edit is mechanical but spans three files. The configuration is not exposed as a CLI flag because the format is structurally enforced; a maintainer who wants a different format (for example, changing the breakpoint from 60 seconds to 90 seconds or rendering at greater precision) edits the helper's body in `scripts/_shared.py` once and both surfaces pick up the change automatically. |
| 23 | `BLITZY_RUN_ID` Validation Constraints (Path-Traversal Defense and Length Cap) | (a) Continue consuming the `BLITZY_RUN_ID` environment variable verbatim with no validation, accepting that values containing `/`, `..`, or other path metacharacters can escape the intended `logs/<run_id>/` scope and write log files in attacker-controlled locations, and accepting that values longer than the filesystem's `NAME_MAX` raise an unhandled `OSError` (Errno 36, File name too long) that crashes the harness; (b) Sanitize the value by replacing every non-conforming character with an underscore and logging a warning when sanitization fires, so any user-supplied value runs to completion but the resulting `run_id` may differ silently from the supplied value; (c) Validate the value against the regex `^[A-Za-z0-9._-]{1,64}$` and reject non-conforming values with a `ValueError` that names the offending environment variable, the observed length, the maximum allowed length, the allowed character class, and the validation regex pattern, without echoing the offending value itself. Each alternative was evaluated against the QA Checkpoint B Medium finding (Issue #2, path-traversal vulnerability) and the QA Checkpoint B Low finding (Issue #3, unhandled `OSError` on excessively long `run_id`) and against the defense-in-depth principle that user-supplied filesystem-path components must be validated before any `Path.mkdir()` call. | Alternative (c): introduce a `VALID_RUN_ID_RE = re.compile(r"^[A-Za-z0-9._-]{1,64}$")` constant plus a `MAX_RUN_ID_LENGTH = 64` constant in `scripts/_shared.py::Section 5b` (`Run ID Validation`); call `VALID_RUN_ID_RE.fullmatch(value)` from `get_or_create_run_id()` before any filesystem operation; raise `ValueError` with a structured message that names the offending environment variable (`BLITZY_RUN_ID`), the observed length, the documented maximum (64), the allowed character class (`A–Z`, `a–z`, `0–9`, `.`, `_`, `-`), and the validation regex pattern, but deliberately does NOT include the offending value itself to prevent log injection through the error-handling path. The UUIDv4 default (used when `BLITZY_RUN_ID` is unset or empty after `strip()`) matches the regex by construction because UUIDv4 strings consist only of `[0-9a-f-]` characters and are exactly 36 characters long. | The path-traversal vulnerability documented in QA Checkpoint B Issue #2 demonstrates that an unvalidated `BLITZY_RUN_ID` value like `evil/path/../../../injection-attempt` causes the harness to create log directories outside the intended `blitzy/reports/acceleration/logs/<run_id>/` scope, which violates the read-only-on-analyzed-repo constraint when the traversal lands on the analyzed repository. The unhandled `OSError` documented in QA Checkpoint B Issue #3 demonstrates that a 500-character `BLITZY_RUN_ID` crashes the harness during `Path.mkdir(parents=True, exist_ok=True)` with `[Errno 36] File name too long` rather than producing a clear validation error. Sanitization (alternative (b)) is unsafe because it silently changes the operator-supplied correlation ID, breaking the audit trail between an external orchestrator and the harness's log directories. The regex `^[A-Za-z0-9._-]{1,64}$` is the smallest character class that admits all expected sources of `run_id` values (UUIDv4 strings, GitHub Actions run identifiers like `12345-67890`, manual operator labels like `qa-checkpoint-B-1`) while rejecting every path metacharacter (`/`, `\`, `:`, `..`, `~`, whitespace, control characters) and bounding the length at 64 characters — comfortably above the 36-character UUIDv4 baseline and well below filesystem `NAME_MAX`. | An external orchestrator that previously supplied an unconstrained `run_id` value (for example, one containing a colon as a timestamp separator, or one longer than 64 characters) will see its harness invocations fail with a `ValueError` rather than running to completion. Mitigation: the rejection message is structured (named variable, observed length, max length, allowed character class, regex pattern) so the orchestrator's logs make the constraint immediately obvious; the README's Common Pitfalls section and the `BLITZY_RUN_ID` row in the Environment Variables Reference table document the constraint inline; the UUIDv4 default remains available (set `BLITZY_RUN_ID=` to the empty string or unset the variable entirely to fall back to the auto-generated UUID). The `_shared.py::Section 5b` docstring also notes that the offending value is deliberately NOT echoed in the error message, which addresses the secondary log-injection concern that a malformed `run_id` could otherwise carry hostile content into operator log streams. | High — the validation regex and the length cap are defined as two constants in `scripts/_shared.py::Section 5b`; relaxing the regex (for example, to admit colons or forward slashes) is a one-line edit, and raising the length cap requires editing both constants in lockstep. A reviewer who wants to disable validation entirely can replace `VALID_RUN_ID_RE.fullmatch(value)` with a no-op truthy check, although doing so would re-introduce both QA Checkpoint B issues that this row was made to resolve. The configuration is not exposed as a CLI flag because the constraint is structural; an environment-variable-only override (for example, `BLITZY_RUN_ID_SKIP_VALIDATION=1`) would defeat the defense-in-depth purpose and is therefore not provided. |
| 24 | Puppeteer/Chrome Infrastructure-Failure Reclassification and Container-Friendly Sandbox Override in `render_diagrams.py` | (a) Continue treating every non-zero exit from `mmdc` (the Mermaid CLI) as a Mermaid-syntax error, accepting that Puppeteer/Chrome launch failures in containerized environments (running as root without `--no-sandbox`, missing shared libraries like `libnss3.so`, missing browser binary) are reported as 17–34 false-positive syntax errors per run and cause the script to exit with status 1 even when every diagram block is syntactically valid; (b) Detect known Puppeteer/Chrome launch-failure stderr signatures and reclassify the failure as `infrastructure_error` rather than `syntax_error`, raise `CLIUnavailable` from `cli_syntax_check()` when the classification is infrastructure, and let the existing `validate_file()` handler fall back to the regex validator so the script exits 0 for syntactically valid blocks; additionally pass `--puppeteerConfigFile` to `mmdc` referencing a JSON file with `{"args": ["--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage"]}` so the sandbox limitation is preempted in container environments where the operator is root; (c) Switch the validator entirely to regex-only by removing the Mermaid CLI invocation path, accepting that syntactically invalid blocks that would have been caught by the headless renderer are no longer detected. Each alternative was evaluated against the QA Checkpoint B Medium finding (Issue #1, `render_diagrams.py` default-mode failure in containerized environments) and against the existing two-tier validator design (CLI primary, regex fallback) that the script's `validate_file()` already implements. | Alternative (b): introduce a `_INFRASTRUCTURE_FAILURE_SIGNATURES` tuple in `scripts/render_diagrams.py` enumerating 26 stable stderr substrings spanning Puppeteer diagnostics (six entries: `failed to launch the browser process`, `failed to launch`, `puppeteer.launch`, `browser was not found at the configured executable`, `could not find chromium`, `could not find browser`), Chrome/Chromium zygote-sandbox diagnostics (three entries: `running as root without --no-sandbox is not supported`, `no usable sandbox`, `the setuid sandbox is not running`), Linux runtime/missing-library diagnostics (six entries: `error while loading shared libraries`, `cannot open shared object file`, `libnss3.so`, `libgbm.so`, `libatk-1.0`, `libgtk-3`), OS spawn failures (four entries: `spawn enoent`, `spawn eacces`, `spawn epipe`, `command not found`), and npm/npx fetch failures (seven entries: `etarget`, `enotcached`, `network timeout`, `fetch failed`, `could not resolve`, `registry returned 4`, `registry returned 5`); add `_classify_cli_failure(stderr_text)` to return `"infrastructure"` or `"syntax"` by substring match (empty stderr defaults to `"syntax"`); add `_write_puppeteer_config(target_dir)` to emit a process-local JSON config containing the `_PUPPETEER_LAUNCH_ARGS = ("--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage")` triple; thread the config-file argument through `cli_syntax_check()` via a `mkdtemp(prefix="mermaid-puppeteer-")` directory cleaned up in a `finally` block; widen `detect_cli_availability()` to return `tuple[bool, str]` so the operator log can carry a specific infrastructure reason; smoke-test the CLI at startup by rendering a minimal `graph TD A --> B` block (which exercises the same Puppeteer config path); on infrastructure classification, raise `CLIUnavailable` and let `validate_file()`'s existing handler fall back to the regex validator. | The QA Checkpoint B Medium finding documents that the default `python3 render_diagrams.py` invocation fails in any containerized environment running as root because Chrome's zygote-sandbox refuses to start without `--no-sandbox`; the existing implementation catches the non-zero exit but reports it as a Mermaid syntax error, which is both a misclassification (the diagrams are valid) and a confusing operator experience (17 false-positive errors per run). The reclassification approach preserves the CLI as the primary validator when the infrastructure permits while keeping the regex fallback as the safety net when it does not; the per-process `--puppeteerConfigFile` JSON eliminates the most common container blocker (root + no `--no-sandbox`) without altering operator-visible behavior on environments where the sandbox already works (the extra flags are no-ops in non-root contexts and on environments where the sandbox is already disabled). Removing the CLI path entirely (alternative (c)) would forfeit the headless renderer's syntactic-validation precision; continuing with the misclassification (alternative (a)) would leave the operator-facing log permanently misleading and would block adoption of the harness in CI/CD pipelines that run containers as root. The infrastructure-signature tuple was assembled from observed stderr text across the QA Checkpoint B reproduction plus the documented Puppeteer and Chrome error catalog, and the substring match is intentionally conservative (any signature match flags infrastructure; a stderr that lacks all signatures is treated as a genuine syntax error so the original validator behavior is preserved for true syntax problems). | A future Puppeteer or Chrome release that emits a previously unseen launch-failure stderr signature (for example, a new error message added by Chrome 200+ in a future container scenario) could be misclassified as a syntax error until the signature is added to `_INFRASTRUCTURE_FAILURE_SIGNATURES`. Mitigation: every signature match and every classification result are logged with the full stderr text via the structured JSON logger, so an operator who sees a new misclassification has the exact stderr text and can update the signature list with one PR; the regex fallback continues to work even if the CLI is misclassified, so the script still exits 0 for syntactically valid blocks; the `_classify_cli_failure()` function is one self-contained helper that a future maintainer can extend without touching the rest of the validator. A secondary risk is that the `--no-sandbox` flag weakens Chrome's process isolation, but this is acceptable because the headless render is only consuming Mermaid source text from the harness's own deliverables (not untrusted external input) and because the alternative is the script not running at all in containerized environments. | High — the infrastructure-signature tuple, the `_classify_cli_failure()` helper, and the `_write_puppeteer_config()` helper are private symbols in `scripts/render_diagrams.py`; adding a new signature is a single-tuple entry; removing the sandbox-override is a single-tuple constant edit; reverting to the pre-fix behavior is a removal of the classification call in `cli_syntax_check()`. The `--no-cli` CLI flag remains available as an explicit operator escape hatch that bypasses the Puppeteer path entirely; the Common Pitfalls section of `README.md` documents the flag for container environments where the operator prefers the regex validator regardless of CLI availability. |
| 25 | HTML Entity Decoding for Mermaid Blocks Extracted from Self-Contained HTML (`executive-presentation.html`) | (a) Extract `<pre class="mermaid">…</pre>` block bodies from `executive-presentation.html` as raw text (including HTML entities like `--&gt;` for `-->`), accepting that the Mermaid CLI's `mmdc` invocation receives HTML-encoded arrows it cannot parse and reports syntax errors that are artifacts of the encoding rather than actual problems in the deck's source; (b) Apply `html.unescape()` (Python stdlib) to every extracted block body before passing it to the validator, mirroring the browser-side behavior where `element.textContent` returns the DECODED text and reveal.js's `<pre class="mermaid">` blocks are parsed as plain Mermaid source; (c) Pre-process `executive-presentation.html` during build time to write decoded copies of every Mermaid block into adjacent `<pre class="mermaid-source">` blocks, accepting the additional artifact and the HTML-template complexity that comes with it. Each alternative was evaluated against the discovery (made during the Row 24 implementation verification) that `extract_mermaid_blocks_from_html()` returned block bodies containing `--&gt;` arrows that the Mermaid CLI rejected; the discovery was previously masked by the Puppeteer launch failure documented in Row 24 (the CLI never reached the lexer in the failing environment) and surfaced only after the Row 24 fix made the CLI launch successfully. | Alternative (b): import `html` at module level in `scripts/render_diagrams.py` and call `html.unescape(match.group("body")).strip()` inside `extract_mermaid_blocks_from_html()` before yielding each block. The decoded text matches what a browser would expose via `element.textContent`, which is the same text that reveal.js's Mermaid integration parses at runtime. The Markdown extractor (`extract_mermaid_blocks_from_markdown()`) does NOT call `html.unescape()` because Markdown fenced code blocks are not subject to HTML entity decoding by Markdown renderers; the asymmetry mirrors the source-language semantics. | The HTML deck contains `<pre class="mermaid">graph TD A --&gt; B</pre>` because HTML requires `>` to be encoded as `&gt;` in text-content adjacent to tag boundaries (the alternative — embedding raw `-->` — would not parse as well-formed HTML in strict XML/XHTML parsers and is fragile across HTML linters). Browsers decode entities automatically when the Mermaid library reads `element.textContent`; the standalone Mermaid CLI has no decoding step and would receive `--&gt;` literally, which it does not recognize as the directed-edge syntax. The Python `html.unescape()` function mirrors browser behavior precisely: it decodes named entities (`&gt;`, `&lt;`, `&amp;`, `&quot;`, `&apos;`) and numeric entities (`&#62;`, `&#x3E;`) using the same WHATWG HTML5 entity table that browsers use. The asymmetry between the HTML extractor (which decodes) and the Markdown extractor (which does not) is correct: Markdown fenced code blocks are parsed verbatim by Markdown renderers and are not subject to HTML entity decoding at any layer. Alternative (c) (pre-process the HTML to add decoded source blocks) adds a build-time pass and a doubled-content payload to the deck for no benefit beyond what the in-validator decoding already provides. | A future contributor who embeds a Mermaid block in HTML using unescaped angle brackets (`-->` directly in the `<pre>` body) would still produce a decoded value that matches the original source, because `html.unescape()` is a no-op on text that contains no entities. The decoding is therefore safe across both encoding styles. A secondary risk is that a future Mermaid syntax that uses `&amp;` or `&lt;` as a literal character (rather than an entity) would be misinterpreted as encoded text; this risk is theoretical because Mermaid's actual syntax does not use HTML entities as literal characters, and any future syntax change would be detected by the existing regex validator's syntax check before the CLI is invoked. Mitigation: the `extract_mermaid_blocks_from_html()` docstring documents the decoding step and notes the browser-parity rationale; the Markdown extractor's lack of decoding is similarly documented; the decoding is observable via the structured JSON log line emitted by the extractor when it processes each block. | High — the change is a one-line `html.unescape()` call inside `extract_mermaid_blocks_from_html()` plus a module-level `import html` from the Python standard library. Reversing the choice (for example, to accept raw HTML-encoded blocks that the CLI cannot parse) is a one-line removal, although doing so would re-introduce the false-positive syntax errors that Row 25 was made to resolve. The configuration is not exposed as a flag because the decoding is structurally correct for any HTML source; if a future deck format embeds Mermaid blocks in a different surface (for example, JSON), a new extractor function would be added rather than the existing one being altered. |

The twenty-five decisions above cover the inflection-detection step (Row 1), window-alignment arithmetic (Rows 2 and 16), the four cross-cutting concerns that affect every metric (Rows 11, 12, 13, and 14), the per-metric algorithm choices for M6, M8, M9, M10, and M12 (Rows 3, 4, 5, 6, 7, 8, and 9), the bot-exclusion list that affects M1 and M2 (Row 10), the per-module aggregation strategy required by the monorepo structure (Row 15), the API-caching strategy that affects reproducibility (Row 17), the M6 placeholder aliasing rule that resolves the distribution-vs-scalar mismatch in single-value surfaces (Row 18), the pre-baked-rows design choice for the Requirements Traceability Matrix (Row 19), the Visual Architecture diagram substitution for this non-migration deliverable (Row 20), the CDN dependency pinning and SRI compensating-controls strategy for the executive deck (Row 21), the cross-surface duration formatting policy that resolves the deck-versus-report drift for M4 and M7 (Row 22), the `BLITZY_RUN_ID` validation constraints that resolve the path-traversal and long-name issues (Row 23), the Puppeteer infrastructure-failure reclassification and container-friendly sandbox override in `render_diagrams.py` (Row 24), and the HTML entity decoding step in `extract_mermaid_blocks_from_html()` (Row 25). The original seventeen mandatory rows plus the three rows appended in response to QA Checkpoint 1 (Rows 18, 19, and 20) plus the two rows appended in response to QA Checkpoint 2 (Row 21 for the Mermaid 11.4.0 vulnerability and SRI omission with compensating controls; Row 22 for the cross-surface duration formatter that resolves the Rule 4 finding) plus the three rows appended in response to QA Checkpoint B (Row 23 for `BLITZY_RUN_ID` validation, Row 24 for the Puppeteer infrastructure reclassification, and Row 25 for the HTML entity decoding) constitute the full decision inventory at this checkpoint. No mandatory decision is omitted; any decision not enumerated above either was trivial enough that any competent engineer would have made the same choice, or has not yet surfaced during implementation.

### Implementation Anchors

Each decision above is implemented at a specific location in the harness so a future maintainer who wants to inspect, alter, or reverse a choice can find the code directly. The anchors below map each row number to its primary implementation file and the function or constant that encodes the decision. Secondary implementation sites (call sites, test sites, documentation references) are reachable from the primary anchor by static analysis. Where a decision is implemented across multiple files, the primary site is the one that contains the decision's branching logic; the other sites are downstream consumers.

- **Row 1 — Inflection Detection Method.** Primary anchor: `scripts/derive_inflection.py::detect_inflection()`. The function computes both the author-identity candidate and the velocity candidate, reconciles them within the 30-day tolerance, and writes the chosen date and both candidates to `data/inflection.json`. The CLI flags `--method co-author|velocity|both` and `--tolerance-days N` expose the decision parameters.
- **Row 2 — Window Boundary Handling.** Primary anchor: `scripts/generate_windows.py::assign_boundary_window()`. The function implements the majority-of-days rule and is selected via the `--boundary-method` flag; the three implementations (majority, split, start) are factored as separate functions and dispatched by the flag value.
- **Row 3 — Flow Distribution Classification Waterfall.** Primary anchor: `scripts/extract_metrics.py::classify_flow_distribution()`. The function calls Tier 1 (`classify_by_label`), Tier 2 (`classify_by_prefix`), and Tier 3 (`classify_by_keyword`) in sequence and returns the first non-`None` result. The label-to-category map is the `LABEL_CATEGORY_MAP` constant in `scripts/_shared.py`.
- **Row 4 — Revert Attribution Algorithm.** Primary anchor: `scripts/extract_metrics.py::attribute_revert()`. The function tries `parse_reverts_commit_line()` first and falls back to `tree_hash_lookup()`; unattributable reverts are tagged in the returned tuple and counted into the metric's sub-counts.
- **Row 5 — Reverts-of-Reverts Exclusion.** Primary anchor: `scripts/extract_metrics.py::attribute_revert()` (same function as Row 4). The recursive check for reverts-of-reverts is the first conditional after the original commit is identified; the conditional can be removed to restore inclusive behavior.
- **Row 6 — Release Source Fallback Precedence.** Primary anchor: `scripts/extract_metrics.py::get_releases()`. The function calls `github_releases_api()` first, then `git_annotated_tags()`, then `ci_deployment_events()`; the first non-empty result is the authoritative source and is recorded in `data/metric_9.json#source`.
- **Row 7 — Prerelease Exclusion.** Primary anchor: `scripts/_shared.py::is_prerelease(name)`. The function returns `True` if the tag name matches any of the four documented suffix patterns; the regex pattern is `r'-(alpha|beta|rc|dev)(\.|\d|$)'`.
- **Row 8 — M10 Fallback When Admin Audit Log Unavailable.** Primary anchor: `scripts/extract_metrics.py::extract_approved_exceptions()`. The function checks `audit_log:read` scope at startup via `verify_token_scopes()` and selects between the full-signal extraction path and the label-plus-force-push subset.
- **Row 9 — M12 Fallback When No SLA Source Available.** Primary anchor: `scripts/extract_metrics.py::extract_defects_out_of_sla()`. The function first tries `linear_api_sla_lookup()`, then `repository_policy_search()`; if both return `None`, the function emits `{"status": "insufficient_signal", "reason": "no SLA source"}` to `data/metric_12.json`.
- **Row 10 — Bot Exclusion List.** Primary anchor: `scripts/_shared.py::BOT_LOGINS`. The constant is a frozen set containing `dependabot[bot]`, `github-actions[bot]`, `renovate[bot]`, and `kodiak[bot]`; every author-identity filter in `extract_metrics.py` references this constant.
- **Row 11 — Blitzy Agent Inclusion in Per-Actor Aggregations.** Primary anchor: the absence of `blitzy-agent` from `scripts/_shared.py::BOT_LOGINS` plus the inclusion logic in `scripts/_shared.py::engineering_actor()`. The decision is enforced by what is NOT excluded; reversing it would require adding `blitzy-agent` to `BOT_LOGINS` and replicating the per-actor aggregation logic to exclude its row.
- **Row 12 — Engineering Actor Substitution Selector.** Primary anchor: `scripts/_shared.py::engineering_actor(pr, phase)`. The function is five lines long and has exactly one phase branch; it is called from every per-actor aggregation site in `extract_metrics.py`.
- **Row 13 — Observability Non-Applicabilities.** Primary anchor: this decision-log entry plus the Observability section of `blitzy/reports/acceleration/README.md`. The decision has no code implementation by definition; the documented non-applicability is the implementation.
- **Row 14 — Onboarding Co-location.** Primary anchor: the existence of `blitzy/reports/acceleration/README.md` and the explicit non-modification of the repository root `README.md`. The decision is enforced by the absence of any commit that touches the repository root.
- **Row 15 — Per-Module Aggregation Weighted by Commit Volume.** Primary anchor: `scripts/_shared.py::weight_by_commit_volume()`. The function takes a per-module metric value table and a per-module commit count table, computes weights as `module_commits / total_commits`, and returns the weighted aggregate. The function is called once per metric per phase.
- **Row 16 — UTC Window Boundaries.** Primary anchor: `scripts/_shared.py::TZ = timezone.utc` plus every `datetime` construction in the harness. The constant `TZ` is referenced wherever a timezone is required; replacing the constant changes the behavior globally.
- **Row 17 — Cache-by-Default for GitHub API.** Primary anchor: `scripts/_shared.py::github_api_get(url, params, no_cache=False)`. The function checks the cache directory `data/cache/` for a matching key before issuing the network request; the `--no-cache` CLI flag propagates `no_cache=True` to this function.
- **Row 18 — M6 Placeholder Aliasing Rule.** Primary anchor: `scripts/build_report.py::M6_ALIAS_MAP` (the mapping `{"<M6.baseline>": "<M6.feature_baseline>", "<M6.ramp_up>": "<M6.feature_ramp_up>", "<M6.steady_state>": "<M6.feature_steady_state>", "<M6.after>": "<M6.feature_steady_state>"}`) plus `scripts/validate_consistency.py::check_m6_alias()`. The build script consults the alias map when populating tokens in the Executive Summary, Acceleration Curve, and Traceability Matrix; the validator asserts that aliased tokens render the same value as their canonical source. Row 18 also documents the visible row label ("M6 Flow Distribution (feature)" in the Acceleration Curve and "Flow Distribution (feature share)" in the Executive Summary) that makes the alias readable in the rendered output.
- **Row 19 — Pre-Baked Per-Metric Rows in Requirements Traceability Matrix.** Primary anchor: the Requirements Traceability Matrix section of `blitzy/reports/acceleration/acceleration-report.md` (the 12 pre-baked rows for M1 through M12) plus `scripts/validate_consistency.py::check_traceability_alignment()`. The matrix template encodes one row per metric; the validator iterates over each row and asserts that the rendered values match the canonical `metrics_results` dictionary populated from `data/metric_*.json`. The choice to pre-bake rows rather than use a single `<traceability_rows>` block placeholder is documented in Row 19's Rationale cell.
- **Row 20 — Visual Architecture Diagram Choice for Non-Migration Deliverable.** Primary anchor: the Methodology section of `blitzy/reports/acceleration/acceleration-report.md`, specifically the Mermaid block titled "Temporal Phase Boundary." The diagram has a descriptive title, a legend embedded as a Mermaid `%%` comment block, and is referenced by name in surrounding prose per the Visual Architecture rule's other clauses. The decision has no code implementation by definition; the substitute diagram itself is the implementation, and the analytical-not-migration scope of the deliverable is what mandates the substitution rather than a literal before/after rendering.
- **Row 21 — CDN Dependency Pinning and Subresource Integrity Strategy.** Primary anchor: `scripts/build_presentation.py::REQUIRED_MERMAID_VERSION = "11.10.0"` plus the CDN `<script>` tag at line 1739 of the same file that emits `https://cdn.jsdelivr.net/npm/mermaid@11.10.0/dist/mermaid.min.js`. The build-time enforcement is in `scripts/build_presentation.py::validate_cdn_versions()`, which scans the rendered deck for the pinned URL and fails the build on any drift. The `crossorigin="anonymous"` and `referrerpolicy="no-referrer"` attributes are emitted on every CDN-loaded `<link>` and `<script>` tag in the rendered `executive-presentation.html`; a future maintainer who wants to enforce SRI in the browser computes the SHA-384 hash of each pinned URL in an internet-enabled environment and adds the `integrity="sha384-..."` attribute to the three CDN `<script>` tags without further HTML changes. The recommended Content Security Policy `script-src 'self' https://cdn.jsdelivr.net https://unpkg.com https://fonts.googleapis.com` allowlist is documented in this row's Rationale cell and is the deployment-environment counterpart to the build-time URL pin.
- **Row 22 — Cross-Surface Duration Formatting Policy.** Primary anchor: `scripts/_shared.py::Section 17b` containing the `format_duration_seconds(value)` and `is_duration_seconds_metric(metric_data)` helpers. The helpers are imported by `scripts/build_presentation.py` (with an HTML-escaping wrapper for string inputs) and by `scripts/build_report.py` (which routes its `duration_phase_fields` frozenset through `format_duration_seconds` inside `_build_substitution_map()`). The structural enforcement is that both renderers consume the same `_shared.py` symbols; any change to the helper's body propagates to both surfaces in a single edit. The `duration_phase_fields` frozenset (`baseline`, `ramp_up`, `steady_state`, `post_intro`, `after`) names the fields that are routed through the formatter; metrics whose `unit` field equals `"seconds"` (M4 Flow Active and M7 Flow Time in this implementation) are detected by `is_duration_seconds_metric()` and trigger the routing.
- **Row 23 — `BLITZY_RUN_ID` Validation Constraints.** Primary anchor: `scripts/_shared.py::Section 5b` (`Run ID Validation`) containing the constants `VALID_RUN_ID_RE = re.compile(r"^[A-Za-z0-9._-]{1,64}$")`, `MAX_RUN_ID_LENGTH = 64`, and `RESERVED_RUN_IDS = frozenset({".", ".."})`, plus the two-stage validation inside `get_or_create_run_id()`: it first invokes `VALID_RUN_ID_RE.fullmatch(value)` and then checks `value in RESERVED_RUN_IDS`, raising `ValueError` on either failure before any filesystem operation. The validation site is intentionally placed before `Path(LOGS_DIR / _RUN_ID_CACHE).mkdir(parents=True, exist_ok=True)` so a malformed `BLITZY_RUN_ID` cannot reach the filesystem. The two-stage structure exists because the regex's character class admits the literal `.` (required for UUIDv4 fragments and semver-style labels), so the single-component strings `"."` and `".."` would otherwise pass the regex and resolve, through `Path(LOGS_DIR / "..")`, to `LOGS_DIR.parent` — a one-level escape that the `RESERVED_RUN_IDS` post-check structurally prevents. The `ValueError` message names the offending environment variable, the observed length, the documented maximum (64), the allowed character class (`A–Z`, `a–z`, `0–9`, `.`, `_`, `-`), and the validation regex pattern for character-class failures; reserved-name rejections instead name the specific category (`"."` or `".."`) and explain why the value would resolve outside `logs/<run_id>/`. Neither error path echoes the offending value itself; the omission is documented in the Section 5b docstring and is the anti-log-injection control. The UUIDv4 default path (used when `BLITZY_RUN_ID` is unset or empty after `strip()`) matches the regex by construction because UUIDv4 strings consist only of `[0-9a-f-]` characters and are exactly 36 characters long, and a UUIDv4 string is never a member of `RESERVED_RUN_IDS`.
- **Row 24 — Puppeteer Infrastructure-Failure Reclassification and Container-Friendly Sandbox Override.** Primary anchor: `scripts/render_diagrams.py` with three new private symbols (`_INFRASTRUCTURE_FAILURE_SIGNATURES` tuple, `_PUPPETEER_LAUNCH_ARGS` tuple, and the helper functions `_classify_cli_failure(stderr_text)` and `_write_puppeteer_config(target_dir)`), plus three modified functions (`cli_syntax_check()` now writes a per-process Puppeteer config under `mkdtemp(prefix="mermaid-puppeteer-")` and threads `--puppeteerConfigFile` through to `mmdc`; `detect_cli_availability()` is widened to return `tuple[bool, str]` and performs a smoke-test render of a minimal `graph TD A --> B` block; `validate_file()`'s existing `CLIUnavailable` handler now records `failure_class: "infrastructure"` and `fallback_validator: "regex"` in its structured log context). The `_INFRASTRUCTURE_FAILURE_SIGNATURES` tuple is the single substring-match table consulted by `_classify_cli_failure()`; any new launch-failure signature is added as a single new tuple entry without modifying the calling code. The `--no-cli` CLI flag is the explicit operator escape hatch that bypasses the Puppeteer probe entirely; the Common Pitfalls section of `blitzy/reports/acceleration/README.md` documents the flag for container environments where the operator prefers the regex validator regardless of CLI availability.
- **Row 25 — HTML Entity Decoding for HTML-Extracted Mermaid Blocks.** Primary anchor: `scripts/render_diagrams.py::extract_mermaid_blocks_from_html()` containing the call `body = html.unescape(match.group("body")).strip()` and the module-level `import html` from the Python standard library. The decoding mirrors the browser-side behavior where `element.textContent` returns the DECODED text of a `<pre class="mermaid">` block; the standalone Mermaid CLI has no decoding step, so the harness applies the decoding explicitly before feeding the block to `mmdc`. The asymmetry with `extract_mermaid_blocks_from_markdown()` (which does NOT decode) is intentional and reflects source-language semantics: Markdown fenced code blocks are parsed verbatim by Markdown renderers without any HTML entity decoding layer. The decoding is observable via the structured JSON log line emitted by the extractor when it processes each block; the docstring in the extractor explains the browser-parity rationale.

### Risk Mitigation Index

Every Risks cell in the Decisions table pairs a risk with a mitigation. The index below cross-references each mitigation to its implementation site so a reviewer can verify that the mitigation is actually in place. A risk without an implemented mitigation would be a defect; this index makes verification straightforward.

- **Row 1 (history rewrite shifts inflection).** Mitigation site: `derive_inflection.py::reconcile_candidates()` emits both candidates and the divergence-days field to `data/inflection.json` whenever the difference exceeds the tolerance; the reviewer-adjudication path is documented in the Methodology section of `acceleration-report.md`.
- **Row 2 (boundary window misclassification).** Mitigation site: `data/windows.json#boundary_window` records the boundary window's start date, end date, and assigned phase; the Methodology section calls out the boundary window explicitly.
- **Row 3 (label-prefix mismatch classification).** Mitigation site: `data/metric_6.json#unknown_rate` per phase; the M6 deep-dive's confidence callout triggers when the rate exceeds 20%.
- **Row 4 (squash-merge unattributable rate).** Mitigation site: `data/metric_8.json#sub_counts.unattributable_rate`; the figure is surfaced in the dashboard and the M8 deep-dive.
- **Row 5 (revert-of-revert misidentification).** Mitigation site: `logs/<run_id>/metric_8.log` records every revert classification, including the recursive check result; the exclusion rate is auditable from the log.
- **Row 6 (CI fallback weakens M9 signal).** Mitigation site: `data/metric_9.json#confidence` is set to `Low` when the CI fallback is used; the caveat callout precedes the M9 cell in the report and the deck.
- **Row 7 (excluded prerelease was effectively a production deploy).** Mitigation site: `data/metric_9.json#sub_counts.prerelease_count` is reported alongside the headline figure; a reader can add the sub-count back to the headline for an inclusive view.
- **Row 8 (Low-confidence M10 misread as authoritative).** Mitigation site: the caveat callout enforced by `build_report.py` plus the risk-assessment section that calls out the audit-log gap explicitly.
- **Row 9 (M12 unmeasured leaves a gap).** Mitigation site: the Risk Assessment section of `acceleration-report.md` enumerates the SLA-source search locations and explains the absence of a numeric value; the Limitations section reinforces the gap.
- **Row 10 (future bot added without list update).** Mitigation site: `scripts/_shared.py::BOT_LOGINS` is documented with the source of each entry (`.kodiak.toml::auto_approve_usernames` plus well-known identifiers); the per-actor breakdown surfaces unexpected high-volume contributors.
- **Row 11 (Blitzy miscounted in per-engineer normalization).** Mitigation site: the per-engineer view in `acceleration-report.md` uses an italicized row style for Blitzy with a footnote naming it as the AI engineering actor; the normalization formula is printed alongside the table.
- **Row 12 (phase-branch leak in selector).** Mitigation site: `scripts/validate_consistency.py::check_actor_disjointness()` asserts the baseline actor set is disjoint from the after-period Blitzy row; any leak fails the consistency check at build time.
- **Row 13 (reader expects service-style observability).** Mitigation site: the `README.md` Observability section enumerates which observability artifacts ARE delivered and explains the non-applicability of the others.
- **Row 14 (reader misses co-located onboarding).** Mitigation site: this Deviations section's cross-reference plus the README's prominence in the deliverable directory listing.
- **Row 15 (cross-cutting commit miscount).** Mitigation site: `data/metric_6.json#cross_module_commits` records the count of cross-cutting commits per phase; the M6 per-module breakdown's caveat callout triggers on high rates.
- **Row 16 (UTC vs. author-local intuition mismatch).** Mitigation site: the Methodology section documents the UTC choice; the per-engineer view groups by UTC date for consistent grouping across reviewer locales.
- **Row 17 (stale cache entry).** Mitigation site: cache keys include the URL and query string, so any new query naturally misses the cache; the `--no-cache` flag forces fresh fetches; the run log records every cache hit and miss.
- **Row 18 (M6 alias drift between Executive Summary and Deep-Dive).** Mitigation site: `scripts/validate_consistency.py::check_m6_alias()` asserts that `<M6.baseline>` equals `<M6.feature_baseline>` (and the same for `ramp_up`, `steady_state`, `after`) at build time; the build fails on any mismatch. The Acceleration Curve row label ("M6 Flow Distribution (feature)") and the Executive Summary column header ("Flow Distribution (feature share)") surface the alias in the rendered output so a reader sees which category is being shown.
- **Row 19 (drift between pre-baked traceability rows and source-of-truth dictionary).** Mitigation site: `scripts/validate_consistency.py::check_traceability_alignment()` iterates over each of the 12 pre-baked rows and asserts that the rendered `multiplier` and `confidence` values match the values in the canonical `metrics_results` dictionary populated from `data/metric_*.json`. The check fails the build on any mismatch and identifies the specific row that drifted, which is more actionable than a block-substitution validation could produce.
- **Row 20 (reader expects literal before/after architecture diagram).** Mitigation site: Deviation 3 in the Deviations section explicitly documents the rule's intent, the non-migration scope conflict, and the chosen substitution; the temporal-phase boundary diagram in the Methodology section of `acceleration-report.md` is named, legended, and referenced in surrounding prose so the Visual Architecture rule's other clauses are satisfied literally. A reader who consults either the decision row or the deviation entry sees the substitution rationale immediately rather than inferring a missing diagram.
- **Row 21 (CDN supply-chain compromise replaces served Mermaid 11.10.0 with malicious payload).** Mitigation site: `scripts/build_presentation.py::REQUIRED_MERMAID_VERSION` enforces the version pin at build time; `scripts/build_presentation.py::validate_cdn_versions()` fails the build on any URL drift; the `crossorigin="anonymous"` attribute is already in place on every CDN tag in the rendered `executive-presentation.html` so SRI enforcement is one `integrity="sha384-..."` attribute away from full activation; the recommended Content Security Policy `script-src` allowlist documented in this row's Rationale cell constrains the deployment-environment execution surface; and the npm-registry SHA verification at the jsdelivr/unpkg origin provides supply-chain integrity at the CDN source. A reader who consults this row sees the compensating-controls layering and the path to full SRI enforcement explicitly.
- **Row 22 (deck-versus-report duration formatting drift for M4 and M7).** Mitigation site: `scripts/_shared.py::Section 17b` is the single canonical implementation of `format_duration_seconds`; both renderers import the same symbol so a divergence requires editing both renderers and the shared module simultaneously, which is detectable by code review. The structural invariant (both surfaces consume the same helper) is the primary defense; a future re-run may additionally add `scripts/validate_consistency.py::check_duration_formatting_parity()` to assert at build time that M4 and M7 phase values render identically across surfaces, although the parity assertion is redundant with the shared-helper invariant and is deferred to the Future Watchpoints subsection.
- **Row 23 (path-traversal injection through `BLITZY_RUN_ID` and `OSError` crash on excessively long values).** Mitigation site: `scripts/_shared.py::Section 5b` validates every operator-supplied `BLITZY_RUN_ID` value in two stages before any filesystem operation. Stage one is the `VALID_RUN_ID_RE.fullmatch(value)` allow-list check: values containing `/`, `\`, `:`, `~`, whitespace, control characters, or any character outside `[A-Za-z0-9._-]`, or values longer than 64 characters, are rejected with a structured `ValueError` that names the variable, the observed length, the maximum allowed length, the allowed character class, and the validation regex pattern. Stage two is the `value in RESERVED_RUN_IDS` post-check: even values that pass the regex (because the regex admits the literal `.`) are rejected if they are the single-component filesystem-reserved strings `"."` or `".."`. Stage two exists because `Path(LOGS_DIR / "..")` resolves to `LOGS_DIR.parent` — a one-level escape from the intended `logs/<run_id>/` scope — and `Path(LOGS_DIR / ".")` resolves back to `LOGS_DIR` itself, contaminating multi-run log isolation. The UUIDv4 default produced by `uuid.uuid4().hex[:32]` (or its full string form) passes both stages by construction and remains the fallback when `BLITZY_RUN_ID` is unset or empty after `strip()`. Neither error message echoes the offending value; the omission is documented in the Section 5b docstring as the anti-log-injection control. A reader who consults the structured logger output sees the constraint violation without seeing the hostile content. The README's Environment Variables Reference table and the Common Pitfalls section document the constraint inline so an external orchestrator (CI/CD pipeline, manual operator) configures its `BLITZY_RUN_ID` value within the constraint from the outset.
- **Row 24 (Puppeteer/Chrome launch failure misclassified as Mermaid syntax error in containerized environments).** Mitigation site: `scripts/render_diagrams.py::_classify_cli_failure(stderr_text)` consults the `_INFRASTRUCTURE_FAILURE_SIGNATURES` substring-match tuple to distinguish infrastructure failures from genuine syntax errors; on infrastructure classification, `cli_syntax_check()` raises `CLIUnavailable` and the existing `validate_file()` handler falls back to the regex validator, so syntactically valid Mermaid blocks no longer produce false-positive syntax errors. The per-process Puppeteer config emitted by `_write_puppeteer_config(target_dir)` passes `--no-sandbox`, `--disable-setuid-sandbox`, and `--disable-dev-shm-usage` to Chrome via the `mmdc --puppeteerConfigFile` flag, which preempts the most common container blocker (root execution without `--no-sandbox`). The smoke-test render performed by `detect_cli_availability()` at startup catches infrastructure failures before the validator processes any real diagrams, so the operator sees a single early log line identifying the infrastructure problem rather than per-diagram false-positive entries. The `--no-cli` CLI flag is the explicit operator escape hatch documented in the README's Common Pitfalls section; an operator who wants the regex validator regardless of CLI availability invokes `python3 render_diagrams.py --no-cli`. Every classification result is logged with the full stderr text via the structured JSON logger so an operator who encounters a previously unseen launch-failure signature has the exact text needed to extend `_INFRASTRUCTURE_FAILURE_SIGNATURES`.
- **Row 25 (HTML-encoded Mermaid arrows misparsed by the standalone Mermaid CLI).** Mitigation site: `scripts/render_diagrams.py::extract_mermaid_blocks_from_html()` applies `html.unescape()` to every extracted block body before yielding it; the decoded text matches what `element.textContent` exposes to the browser-side Mermaid library at deck-render time, so the CLI and the browser see the same source. The decoding is observable via the structured JSON log line emitted by the extractor when it processes each block; the docstring documents the browser-parity rationale. The asymmetry with `extract_mermaid_blocks_from_markdown()` (which does NOT decode) reflects source-language semantics — Markdown fenced code blocks are parsed verbatim — and the asymmetry is itself documented in both extractors' docstrings so a future maintainer who adds a new extractor for a different surface (for example, JSON or YAML) makes the correct decoding choice without having to infer the rationale from the call sites.

## Deviations from Literal Interpretation

The user's instructions are followed literally throughout the implementation except where two instructions conflict directly. In every such case, the resolution is documented as a decision-log entry above and is cross-referenced in the bulleted list below. The Explainability rule states that "any deviation from a literal or obvious interpretation of the requirements MUST have an explicit entry in the decision log" and that "unexplained deviations are treated as defects"; the three deviations enumerated below are the only deviations identified during implementation, and each is grounded in an explicit conflict between two user-stated requirements rather than in an interpretive judgment.

The pattern in every deviation is the same: a user-stated rule cannot be literally satisfied because another user-stated rule prohibits the literal satisfaction. In each case, the resolution honors the intent of the constrained rule (Onboarding intent, Observability intent, Visual Architecture intent) while respecting the binding rule (read-only constraint, batch-tool architecture, non-migration deliverable scope). The conflict, the intent, and the chosen resolution are documented so a reader can verify that no rule was silently abandoned.

- **Deviation 1 — Onboarding rule vs. read-only constraint.** The Onboarding rule directs implementers to "update existing onboarding docs (README, setup guides, wikis)" so that a new developer can go from a clean machine to a running application without asking questions. The user's separate boundary rule directs that the analyzed repository "MUST NOT" be modified. The repository's root `README.md` is an existing onboarding document; updating it would satisfy the Onboarding rule but violate the read-only constraint. **Resolution:** Co-locate new onboarding inside the report directory at `blitzy/reports/acceleration/README.md`. The co-located file is itself substantive (Purpose, Quickstart, Domain Context, Common Pitfalls, Architecture, Suggested Next Tasks) and is discovered immediately by any reader who navigates to the deliverable directory. See **Decision Row 14** above for the full rationale, alternatives evaluated, and reversibility analysis. The choice is Low-reversibility because the only path back is the user relaxing the read-only constraint.

- **Deviation 2 — Observability rule's prescriptive list vs. batch-tool architecture.** The Observability rule prescribes "structured logging with correlation IDs, distributed tracing across service boundaries, a metrics endpoint, health/readiness checks, and a dashboard template" as the minimum observability surface for every deliverable. Three of those five items (distributed tracing, metrics endpoint, health/readiness checks) presuppose a long-running service with HTTP surfaces and service-to-service calls; the harness is a batch CLI tool that runs to completion and exits, with no service surface to instrument. **Resolution:** Implement the two items that apply to a batch tool (structured JSON logging with `run_id` correlation IDs in `logs/<run_id>/` and a dashboard template in `dashboard.md`); document the non-applicability of the other three items with rationale rather than treating their absence as silent omission. See **Decision Row 13** above for the full rationale, alternatives evaluated, and reversibility analysis. The choice is Medium-reversibility because adding the three non-applicable items would require restructuring the harness as a long-running process.

- **Deviation 3 — Visual Architecture rule's before/after view requirement vs. non-migration scope.** The Visual Architecture rule states that "if the deliverable modifies an existing architecture, both states MUST be shown — never target-state alone," implying that migrations and refactors must show before/after architecture diagrams. This deliverable is an analytical measurement, not a migration or a refactor; there is no architectural change to depict, and a literal before/after diagram would compare two identical states. **Resolution:** Substitute a temporal-phase boundary diagram in `acceleration-report.md` (Methodology section) that shows the Baseline / Ramp-Up / Steady State boundary and the Monday-aligned window structure. The diagram preserves the rule's intent (depict the temporal dimension of the analysis visually rather than in prose) while respecting that no architectural change exists to compare. The diagram is referenced by name in surrounding text per the rule, and a legend is embedded as a Mermaid `%%` comment block. See **Decision Row 20** above for the full rationale, alternatives evaluated, and reversibility analysis. The choice is Low-reversibility because reversing it would require fabricating an architectural change that does not exist or declaring the deliverable a migration, which would be inaccurate.

Each deviation entry above cross-references the corresponding decision row by number and topic. No additional deviations were identified during implementation; if a future re-run uncovers a new conflict between two user-stated requirements, it will be recorded as a fourth deviation entry with the same structure (rule, conflict, resolution, decision-row cross-reference).

### Conflict Diagnosis Pattern

The three deviations share a common diagnosis pattern. In each case, two user-stated rules apply to the same artifact (a documentation file, the harness architecture, or a diagram requirement); the rules cannot both be satisfied literally; one rule is a hard constraint (read-only on the repository, batch-tool architecture, non-migration scope) and the other is an objective with multiple possible satisfactions (onboarding intent, observability intent, visual-architecture intent). The resolution in each case honors the hard constraint and satisfies the objective by an alternative means that is documented here as a deviation.

The pattern is consistent: identify the conflict explicitly, name the hard constraint, name the objective, choose a resolution that honors both, document the choice as a deviation, and cross-reference the decision row that captures the implementation choice. This pattern is repeatable for any future conflict that arises during a re-run of the harness; new deviations follow the same template (rule, conflict, hard constraint, objective, resolution, decision-row cross-reference).

### Resolution Verification

Each deviation's resolution is verified by a concrete check that a reader can execute independently of the decision log. The verification checks are listed below in the same order as the deviations.

- **Verification for Deviation 1 (Onboarding co-location).** A reader can verify the resolution by: (a) running `git log --oneline -- README.md` on the analyzed repository and confirming that no commit authored by the harness modifies the root `README.md`; (b) opening `blitzy/reports/acceleration/README.md` and confirming the six sections (Purpose, Quickstart, Domain Context, Common Pitfalls, Architecture, Suggested Next Tasks) are present; (c) following the Quickstart steps on a clean machine and confirming the harness runs to completion.
- **Verification for Deviation 2 (Observability non-applicabilities).** A reader can verify the resolution by: (a) running the harness once and confirming that `logs/<run_id>/` is populated with structured JSON log lines that each include a `run_id` correlation field; (b) opening `blitzy/reports/acceleration/dashboard.md` and confirming the twelve-row KPI table is present with thresholds; (c) confirming the absence of any `/metrics` endpoint, `/healthz` probe, or service binary in the harness directory.
- **Verification for Deviation 3 (Temporal-phase diagram substitution).** A reader can verify the resolution by: (a) searching `acceleration-report.md` for the Methodology-section Mermaid diagram titled "Temporal Phase Boundary" or equivalent and confirming the diagram is referenced by name in surrounding prose; (b) confirming the diagram includes a legend in a Mermaid `%%` comment block; (c) confirming no before/after architecture diagram is mistakenly included that would imply a migration.

## Reversibility Summary

The twenty-five decisions span the full range of reversibility tags. Nineteen are tagged "High" because they can be reversed by re-running the harness with a different command-line flag, by editing a single constant or function, or by supplying a different credential at startup. Three are tagged "Medium" because they require a localized code edit but the edit is contained to a single function or a single subsystem. Three are tagged "Low" because they are definitional choices rooted in the user's verbatim instructions or in the deliverable's analytical-not-migration scope; reversing them would invalidate the methodology and would require the user to explicitly relax the binding instruction or to re-frame the analysis.

The nineteen High-reversibility decisions are Rows 1 (Inflection Detection Method), 2 (Window Boundary Handling), 4 (Revert Attribution Algorithm), 5 (Reverts-of-Reverts Exclusion), 6 (Release Source Fallback Precedence), 7 (Prerelease Exclusion), 8 (M10 Fallback When Admin Audit Log Unavailable), 9 (M12 Fallback When No SLA Source Available), 10 (Bot Exclusion List for M1 and M2), 12 (Engineering Actor Substitution Selector), 15 (Per-Module Aggregation Weighted by Commit Volume), 16 (UTC Window Boundaries), 17 (Cache-by-Default for GitHub API), 18 (M6 Placeholder Aliasing Rule), 19 (Pre-Baked Per-Metric Rows in Requirements Traceability Matrix), 21 (CDN Dependency Pinning and Subresource Integrity Strategy), 23 (`BLITZY_RUN_ID` Validation Constraints), 24 (Puppeteer Infrastructure-Failure Reclassification and Container-Friendly Sandbox Override), and 25 (HTML Entity Decoding for HTML-Extracted Mermaid Blocks). For each of these, a single flag, constant, or credential change is sufficient to re-run the analysis with the alternative selected; no cross-file refactor is required.

The three Medium-reversibility decisions are Row 3 (Flow Distribution Classification Waterfall Ordering), Row 13 (Observability Non-Applicabilities), and Row 22 (Cross-Surface Duration Formatting Policy). Row 3 requires editing the classification-waterfall sequence inside `classify_flow_distribution()` in `extract_metrics.py`; the edit is contained to one function but cannot be expressed as a configuration toggle. Row 13 requires restructuring the harness as a long-running process to add a metrics endpoint or a health probe; the change is contained to the harness's main loop and the script orchestrator but is more invasive than a single function edit. Row 22 requires removing the import of `format_duration_seconds` from both renderers and inlining the helper into each surface plus deleting `scripts/_shared.py::Section 17b`; the edit is mechanical but spans three files and cannot be expressed as a configuration toggle.

The three Low-reversibility decisions are Row 11 (Blitzy Agent Inclusion in Per-Actor Aggregations), Row 14 (Onboarding Co-location), and Row 20 (Visual Architecture Diagram Choice for Non-Migration Deliverable). Row 11 is a definitional choice rooted in the user's verbatim instruction that "in the after period, Blitzy is treated as the engineering actor"; reversing it would invalidate the methodology and would require re-writing the user's framing of the after period. Row 14 is rooted in the user's read-only constraint on the analyzed repository; reversing it requires modifying the repository root, which the constraint forbids. Row 20 is rooted in the deliverable's analytical-not-migration scope; reversing it would require fabricating an architectural change that does not exist or declaring the deliverable a migration, which would be inaccurate. Of the three Low-reversibility decisions, Rows 14 and 20 are documented in the Deviations section above (as Deviations 1 and 3 respectively) because each resolves a conflict between two user-stated requirements; Row 11 is a direct implementation of the user's verbatim engineering-actor framing and therefore is not a deviation.

Total decisions recorded: twenty-five rows. The original seventeen rows are documented in the AAP; Rows 18 and 19 were appended in response to QA Checkpoint 1 design-choice findings (Info Issue #4 on M6 placeholder aliasing and Minor Issue #2 on Requirements Traceability Matrix pre-baked rows respectively) to make implicit design choices explicit; Row 20 was appended in response to QA Checkpoint 1 Major Issue DL1 to anchor the temporal-phase diagram substitution noted in Deviation 3; Row 21 was appended in response to QA Checkpoint 2 Major findings on the Mermaid 11.4.0 vulnerability and on the SRI omission in `decision-log.md` and `executive-presentation.html`; Row 22 was appended in response to QA Checkpoint 2 Major finding on cross-surface duration formatting drift for M4 and M7; Rows 23, 24, and 25 were appended in response to QA Checkpoint B findings (Issue #2 path-traversal vulnerability in `BLITZY_RUN_ID`, Issue #3 unhandled `OSError` on excessively long `BLITZY_RUN_ID`, Issue #1 Puppeteer launch failure misclassified as Mermaid syntax error, and the HTML entity decoding gap discovered during the Row 24 implementation verification). Future re-runs of the harness may append additional rows if new non-trivial decisions are surfaced; the mandatory rows are never removed or reordered, and any addition is appended after Row 25. The reversibility summary above will be updated to reflect any added rows; the High/Medium/Low counts in this paragraph reflect the twenty-five recorded rows (19/3/3).

### Per-Decision Reversibility Detail

The paragraphs below expand on the one-sentence reversibility justification in each Decisions-table row. Each paragraph names the concrete reversal procedure (the command, the file edit, or the constraint relaxation) so a future maintainer who wants to reverse a choice does not have to reconstruct the procedure from the implementation code. The paragraphs are grouped by reversibility tier (High, Medium, Low) and within each tier are ordered by row number.

**High-reversibility decisions (nineteen rows).** Each of these can be reversed by a flag, a constant, or a credential change without any cross-file refactor; the reversal procedure is contained to a single command or a single line of code.

- **Row 1 reversal.** Run `python scripts/derive_inflection.py --method velocity --tolerance-days 30` to switch the authoritative inflection from the author-identity candidate to the velocity candidate. The harness regenerates `data/inflection.json` and propagates the new date to all downstream window assignments deterministically. No code change is required.
- **Row 2 reversal.** Run `python scripts/generate_windows.py --boundary-method split` (or `--boundary-method start`) to switch the boundary-window assignment from majority-of-days to the alternative. The harness regenerates `data/windows.json` and the next metric extraction picks up the new window table automatically.
- **Row 4 reversal.** Edit the order of fallback attempts in `attribute_revert()` (for example, try tree-hash lookup before the body-line parse) or add a third attribution method. The edit is contained to one function in `scripts/extract_metrics.py`; re-run `python scripts/extract_metrics.py --metric 8 --no-cache` to regenerate the M8 figures with the new algorithm.
- **Row 5 reversal.** Remove the recursive `is_revert_of_revert()` conditional in `attribute_revert()` to restore inclusive counting of reverts. The edit is one conditional in one function; re-run `--metric 8` to regenerate the M8 figures.
- **Row 6 reversal.** Edit `get_releases()` to change the precedence order or to skip a source (for example, prefer CI deployments over the API). The edit is contained to one function in `scripts/extract_metrics.py`; re-run `--metric 9 --no-cache` to regenerate the M9 figures with the new source.
- **Row 7 reversal.** Edit the regex pattern in `is_prerelease()` to add or remove suffix patterns, or remove the prerelease filter entirely from `extract_releases()` to count all releases including prereleases. Re-run `--metric 9` to regenerate.
- **Row 8 reversal.** Supply a GitHub Personal Access Token that includes the `audit_log:read` scope and re-run `python scripts/extract_metrics.py --metric 10 --no-cache`. The harness detects the new scope automatically at startup and switches to the full-signal extraction path.
- **Row 9 reversal.** Set the `LINEAR_API_KEY` environment variable to a key with read access to the `blitzy-cal` Linear team and re-run `python scripts/extract_metrics.py --metric 12 --no-cache`. The harness detects the key automatically and queries `teams/{id}/slaPolicies` for SLA targets and `issues?filter[label][name][eq]=bug` for resolution times.
- **Row 10 reversal.** Edit the `BOT_LOGINS` constant in `scripts/_shared.py` to add or remove bot identifiers. Re-run the harness to apply the new filter; the change affects M1, M2, and the per-actor breakdowns of M4, M5, M6, and M10.
- **Row 12 reversal.** Replace `engineering_actor(pr, phase)` with two separate functions (`baseline_actor(pr)` and `after_actor(pr)`) and update every call site to dispatch on phase. The edit is mechanical and contained to `scripts/_shared.py` plus the call sites in `scripts/extract_metrics.py`; the dual-path implementation would re-introduce the drift risk that the single-selector choice was made to avoid.
- **Row 15 reversal.** Replace `weight_by_commit_volume()` with `weight_by_equal()` or with a path-aware multi-module attribution function. The edit is contained to `scripts/_shared.py`; re-run the harness to apply the new weighting.
- **Row 16 reversal.** Change the `TZ` constant in `scripts/_shared.py` to a different `timezone` instance, or refactor the timestamp arithmetic to be author-timezone-aware by reading the offset from each commit's authored timestamp. The first option is a one-line change; the second is a more invasive refactor but is still contained to the timestamp-handling functions.
- **Row 17 reversal.** Pass `--no-cache` to any `python scripts/extract_metrics.py` invocation to force fresh fetches for that run, or `rm -rf data/cache/` to clear the cache permanently. Either action returns the harness to fetch-on-every-call behavior.
- **Row 18 reversal.** Edit `scripts/build_report.py::M6_ALIAS_MAP` to point `<M6.baseline>` (and `<M6.ramp_up>`, `<M6.steady_state>`, `<M6.after>`) at a different category (for example, defect share) by changing the mapped values; correspondingly update the Acceleration Curve row label ("M6 Flow Distribution (feature)") and the Executive Summary column header ("Flow Distribution (feature share)") in `acceleration-report.md` so the visible label matches the aliased category. Re-run `python scripts/build_report.py` to regenerate the report; `scripts/validate_consistency.py::check_m6_alias()` will succeed as long as the alias map is internally consistent. Reverting the entire aliasing rule (alternative (b): use only `<M6.feature_*>` tokens in all surfaces) is a search-and-replace edit in `acceleration-report.md` and a removal of the alias map.
- **Row 19 reversal.** Replace the 12 pre-baked rows in the Requirements Traceability Matrix section of `acceleration-report.md` with a single `<traceability_rows>` block-substitution placeholder; correspondingly update `scripts/build_report.py::populate_traceability_block()` to iterate over `data/metric_*.json` and emit the rows at render time. The reversal is a localized edit to two files and is reversible by re-applying the pre-baked-rows template. The consistency-check function `scripts/validate_consistency.py::check_traceability_alignment()` continues to work in both designs because it consults the same `metrics_results` dictionary.
- **Row 21 reversal.** Edit `scripts/build_presentation.py::REQUIRED_MERMAID_VERSION` to a different pinned version (for example, a future Mermaid release that fixes additional vulnerabilities) and update the CDN URL on the matching `<script>` tag at the documented line; the `validate_cdn_versions()` function picks up the new constant automatically and fails the build if any tag references a different version. To enable SRI enforcement in the browser, compute the SHA-384 hash of each of the three pinned CDN URLs (reveal.js, Mermaid, Lucide) in an internet-enabled environment and add `integrity="sha384-..."` attributes to the three CDN `<script>` tags; the `crossorigin="anonymous"` attribute is already in place on every CDN tag, which is a browser-side prerequisite for SRI enforcement, so no other HTML edit is required to activate the hash check. Removing the `crossorigin` attributes (the alternative-(a) configuration) is a single search-and-replace edit in `scripts/build_presentation.py`; the deck will continue to load but loses the SRI-readiness compensating control.
- **Row 23 reversal.** Edit the `VALID_RUN_ID_RE` and `MAX_RUN_ID_LENGTH` constants in `scripts/_shared.py::Section 5b` to relax or tighten the validation. Relaxing the character class (for example, admitting `:` or `+` as additional allowed characters) is a one-line change to the regex pattern; raising the length cap requires editing both constants in lockstep so the regex bound and the documented maximum agree. Reversing the validation entirely (re-introducing the pre-fix behavior) is a removal of the `VALID_RUN_ID_RE.fullmatch(value)` check inside `get_or_create_run_id()`; the constants can remain defined for documentation purposes even if they are not consulted. The UUIDv4 default fallback path is unaffected by any of these changes because UUIDv4 strings satisfy any reasonable regex that includes hex digits and the hyphen separator.
- **Row 24 reversal.** Three independent reversal axes are available. First, the infrastructure-signature tuple `_INFRASTRUCTURE_FAILURE_SIGNATURES` in `scripts/render_diagrams.py` can be edited to add a new launch-failure substring (a one-tuple-entry change) or to remove an existing signature (also a one-entry change); each entry is independent of the others. Second, the per-process Puppeteer config produced by `_write_puppeteer_config()` and threaded through `cli_syntax_check()` via `--puppeteerConfigFile` can be disabled by removing the `_PUPPETEER_LAUNCH_ARGS` tuple's contents (leaving an empty tuple) or by removing the config-file argument from the `mmdc` invocation; reverting either change re-introduces the sandbox-blocked container behavior. Third, the smoke-test render performed by `detect_cli_availability()` at startup can be reduced back to a `--version` probe by removing the smoke-test block; doing so would re-introduce the false-positive misclassification risk that the smoke test was designed to catch early. The `--no-cli` CLI flag remains the explicit operator escape hatch regardless of these reversals.
- **Row 25 reversal.** Remove the `html.unescape()` call inside `extract_mermaid_blocks_from_html()` in `scripts/render_diagrams.py` and remove the module-level `import html` statement if no other call sites use it. The change is one line plus the import. Doing so would re-introduce the false-positive syntax errors that the decoding was made to resolve, but a future maintainer who controls the source of the HTML (for example, by switching the deck template to emit raw `-->` instead of `--&gt;`) could legitimately remove the decoding step provided every HTML producer adopts the same convention.

**Medium-reversibility decisions (three rows).** Each of these requires a localized code edit that goes beyond a single flag or constant change but is contained to a single function or subsystem.

- **Row 3 reversal.** Reverse the order of tiers in `classify_flow_distribution()` in `scripts/extract_metrics.py` (for example, run Tier 2 conventional-commit prefix first, then Tier 1 labels, then Tier 3 keywords). The edit is contained to one function but cannot be expressed as a configuration toggle without restructuring the function. Re-run `--metric 6 --no-cache` to regenerate the M6 figures with the new waterfall.
- **Row 13 reversal.** Restructure the harness as a long-running process to add a metrics endpoint or a health probe. The change requires introducing an HTTP server, a process-lifecycle manager, and an inter-process communication path between the extraction scripts and the new service surface. The change is contained to the harness's main loop and the script orchestrator but is more invasive than a single function edit; it would also change the deployment model from "run-to-completion CLI" to "long-running service."
- **Row 22 reversal.** Remove the `from _shared import format_duration_seconds, is_duration_seconds_metric` lines from `scripts/build_presentation.py` and `scripts/build_report.py`; copy the helper bodies inline into each renderer with any surface-specific tweaks desired; delete `scripts/_shared.py::Section 17b`. The edit is mechanical but spans three files and re-introduces the cross-surface drift risk that this decision was made to avoid. A cleaner partial reversal (keeping the shared helper but having one renderer apply a surface-specific transformation on top of it) is also possible and would not require deleting Section 17b; the partial reversal would still require a new decision-log row to record the surface-specific divergence.

**Low-reversibility decisions (three rows).** Each of these is a definitional choice rooted in a user-stated requirement or in the deliverable's analytical-not-migration scope; reversing it would require the user to relax the binding instruction or to re-frame the analysis methodology.

- **Row 11 reversal.** Move `blitzy-agent` into `BOT_LOGINS` and replicate the per-actor aggregation logic to suppress the Blitzy row. This procedure would directly contradict the user's verbatim instruction that "in the after period, Blitzy is treated as the engineering actor — the entity producing code on the PR." Reversing the choice requires the user to re-frame the analysis (for example, by directing that Blitzy be treated as a bot whose contributions are excluded), which would invalidate the methodology and the headline acceleration multipliers that the entire deliverable is designed to compute.
- **Row 14 reversal.** Modify the repository root `README.md` to include the harness's onboarding content. The user's read-only constraint explicitly forbids modifications to the analyzed repository; reversing this choice requires the user to relax the constraint. The reversal is also coupled to the deliverable's directory placement: if onboarding moves into the repository root, the entire `blitzy/reports/acceleration/` directory's self-contained nature is broken because the onboarding would point to artifacts outside the analyzed-repo root.
- **Row 20 reversal.** Render literal before/after architecture diagrams in the Methodology section comparing the codebase pre- and post-inflection. This procedure would require fabricating an architectural change that does not exist in the analyzed repository because the analysis itself does not modify the architecture; reversing this choice therefore requires the user to re-frame the deliverable as a migration, which would be inaccurate. The reversal also has a documentation cost: the substitute temporal-phase diagram would still be needed to depict the analytical phases, so a literal before/after rendering would be additive rather than substitutive.

### Audit Trail

The decision log was authored as the implementation progressed; each row was added to the table at the moment the corresponding decision was made and before the implementation that depends on it was written. This authoring discipline ensures that the rationale captured in each row reflects the reasoning at decision time, not a post-hoc justification reconstructed from the resulting code. The Explainability rule's prohibition on embedding rationale in code comments is the structural enforcement of this discipline.

Six review checkpoints were applied during the implementation. First, the seventeen mandatory rows enumerated in the Agent Action Plan were drafted and reviewed against the AAP's verbatim instructions before any extraction code was written. Second, after each metric extraction script was implemented, the corresponding decision-log row was re-read to verify that the implementation matches the recorded choice; any drift between the row and the code was resolved by editing one of the two to match the other, with the choice recorded in this Audit Trail. Third, the consistency validator (`scripts/validate_consistency.py`) cross-checks that the metric values referenced across report sections agree exactly, which indirectly validates that the underlying decisions (about classification, attribution, fallback precedence, and so on) are applied consistently across the data captures. Fourth, QA Checkpoint 1 findings were reviewed against the existing decision log; two design choices that were implicit in the template structure (the M6 placeholder aliasing rule and the pre-baked-rows design for the Requirements Traceability Matrix) were made explicit by appending Rows 18 and 19, and the temporal-phase diagram substitution that anchors Deviation 3 was made explicit by appending Row 20 in response to Major Issue DL1. The append-only discipline was preserved: no original row was modified, and the original Rationale cells for Rows 18 and 19 were rewritten in place only to fit the 2-4 sentence guideline (extended QA history and validation mechanics were moved to the Implementation Anchors and Risk Mitigation Index, where they were already partially documented). Fifth, QA Checkpoint 2 findings were reviewed against the existing decision log; two security and consistency choices that were implicit in the rendered deck and renderer codebases (the Mermaid CDN version pin with SRI omission and the cross-surface duration formatting policy) were made explicit by appending Rows 21 and 22 respectively. Row 21 resolves the QA Checkpoint 2 Major findings on `build_presentation.py` Finding 6 (Mermaid 11.4.0 vulnerability) and on `decision-log.md` (SRI omission undocumented as a non-trivial security decision); Row 22 resolves the QA Checkpoint 2 Major finding on `build_presentation.py` Finding 2 (cross-surface formatting drift for M4 and M7). Sixth, QA Checkpoint B findings were reviewed against the existing decision log; three implementation choices that were implicit in `scripts/_shared.py` and `scripts/render_diagrams.py` (the lack of `BLITZY_RUN_ID` validation, the lack of Puppeteer/Chrome infrastructure-failure classification, and the lack of HTML entity decoding for HTML-extracted Mermaid blocks) were made explicit by appending Rows 23, 24, and 25 respectively. Row 23 resolves QA Checkpoint B Issue #2 (path-traversal vulnerability) and Issue #3 (unhandled `OSError` on excessively long values) by introducing `VALID_RUN_ID_RE` and `MAX_RUN_ID_LENGTH` constants plus the `fullmatch()` validation step in `get_or_create_run_id()`; Row 24 resolves QA Checkpoint B Issue #1 (Puppeteer launch failures misclassified as Mermaid syntax errors in containerized environments) by introducing the `_INFRASTRUCTURE_FAILURE_SIGNATURES` substring-match tuple, the `_classify_cli_failure()` helper, the per-process Puppeteer config emitted by `_write_puppeteer_config()`, and the smoke-test render performed at startup by the widened `detect_cli_availability()`; Row 25 documents the HTML entity decoding step in `extract_mermaid_blocks_from_html()` that surfaced during the Row 24 verification and that was added as part of the same fix to mirror browser-side `element.textContent` semantics. The append-only discipline was preserved across all three checkpoints: no original row was modified, and the supporting subsections (Implementation Anchors, Risk Mitigation Index, Reversibility Summary, Per-Decision Reversibility Detail, Decision Provenance, Runtime Configuration Summary, Rule Grounding Matrix, Quick Reference Card) were updated to incorporate Rows 23, 24, and 25 without altering the entries for Rows 1 through 22.

No row in the Decisions table was modified after the first run of the consistency validator passed (other than the Rows 18 and 19 Rationale-cell trims described above, which preserved the choice but reduced sentence count). Rows 18, 19, and 20 were appended (not inserted) in response to QA Checkpoint 1; Rows 21 and 22 were appended (not inserted) in response to QA Checkpoint 2; Rows 23, 24, and 25 were appended (not inserted) in response to QA Checkpoint B; the row-number stability of Rows 1 through 17 is preserved across all three checkpoints, as is the row-number stability of Rows 18 through 22 across Checkpoint B. Any future modification of an existing row will be recorded as a separate "Decision Amendment" entry appended to this Audit Trail; the original row text and the amendment text will both be preserved so the decision history is auditable. This append-only discipline applies even to typo corrections in the row text because the principle of an immutable decision record is more valuable than a clean diff.

### Worked Example — Row 1 Decision Flow

The flow below traces a single decision (Row 1, Inflection Detection Method) from its origin in the user's instruction to the harness output. The example demonstrates how each part of a Decisions-table row maps to a concrete artifact in the deliverable.

1. **User instruction.** The Agent Action Plan §0.1.4 (Technical Interpretation) states: "To detect the AI tool introduction date, the implementation searches `git log --all --pretty=format:'%H%n%an%n%ae%n%aI%n%B%n---END---'` for the earliest commit whose author is `Blitzy Agent <agent@blitzy.com>` or whose body contains a `Co-authored-by` trailer naming an AI tool." The user also instructs that "as a verification step, the implementation computes a sliding 14-day commit-count window across the repository's full history and identifies the sharpest sustained inflection." This dual-candidate framing is the seed for Row 1.
2. **Decision row.** Row 1 in the Decisions table records the three alternatives evaluated (author-identity, velocity, co-author trailer), the chosen approach (compute both, use author-identity by default, reconcile within 30 days), the rationale (direct identity signal preferred; velocity is the cross-check), the risk (history rewrite could shift the chosen date) with its mitigation (velocity cross-check catches divergence), and the reversibility ("High — re-run with `--method velocity`").
3. **Implementation.** The decision is implemented in `scripts/derive_inflection.py::detect_inflection()`, which reads `git log` output, identifies the earliest commit by `agent@blitzy.com`, computes the sliding-window velocity inflection, reconciles the two candidates, and writes the chosen date to `data/inflection.json`.
4. **Configuration.** The decision's parameters are exposed via the `--method` and `--tolerance-days` CLI flags listed in the Runtime Configuration Summary; a reviewer who wants to reverse the choice runs `python scripts/derive_inflection.py --method velocity`.
5. **Risk mitigation.** The Risk Mitigation Index entry for Row 1 names the mitigation site (`reconcile_candidates()` emits divergence-days to `data/inflection.json`) and the surfacing surface (the Methodology section of `acceleration-report.md`).
6. **Output.** The chosen inflection date appears in `data/inflection.json#chosen_date` and is referenced from the Environment Verification section of `acceleration-report.md`. Every downstream window assignment in `data/windows.json` consumes this date indirectly via the `phase` field.
7. **Audit trail.** The decision row was authored before `detect_inflection()` was written; the function's docstring references the decision row but does not contain the rationale (per the Explainability rule's prohibition on rationale in code comments).
8. **Reversibility.** A reviewer who disputes the choice can supply `--method velocity` and re-run the harness; the harness regenerates `data/inflection.json` and propagates the new date through all downstream windows and metrics deterministically. The original choice and the new choice are both auditable from `data/inflection.json` and `commands.log`.

This worked example illustrates the eight surfaces through which any decision is visible (user instruction, decision row, implementation site, configuration, risk mitigation, output, audit trail, reversal procedure). Every other row in the Decisions table flows through the same eight surfaces; a reviewer who understands one decision in this way understands the pattern for all twenty-five.

### Future Watchpoints

The twenty-five recorded rows cover every non-trivial decision identified during the initial implementation plus the three follow-on entries made in response to QA Checkpoint 1 (Rows 18, 19, and 20), the two follow-on entries made in response to QA Checkpoint 2 (Rows 21 and 22), and the three follow-on entries made in response to QA Checkpoint B (Rows 23, 24, and 25). The list below names categories of future work where new non-trivial decisions are anticipated; if any of these is undertaken, a new row will be appended after Row 25 and this Future Watchpoints subsection will be updated to record the row's addition.

- **Continuous DORA dashboard backed by an OpenTelemetry pipeline.** Moving from a batch harness to a continuous dashboard would require new decisions about retention windows, alerting thresholds, and the choice of OTel collector and back end. Anticipated additions: ~3 rows.
- **Per-PR file-level review-density correlation.** Adding a regression of review duration on PR file count and changed-lines count would require a decision about the regression library (statsmodels vs. scikit-learn vs. hand-rolled), the outlier-handling method, and the multicollinearity treatment. Anticipated additions: ~2 rows.
- **Author-level skill curve over time.** Modeling per-author productivity trajectories would require decisions about the curve-fitting method (Bayesian vs. frequentist, parametric vs. non-parametric) and the handling of authors with sparse history. Anticipated additions: ~2 rows.
- **Cross-repository comparison against other Blitzy-Sandbox repositories.** Aggregating across repositories would require decisions about normalization (per-engineer, per-commit, per-line-of-code), confidence-tier reconciliation across repositories with different data-source availability, and the treatment of cross-repository contributors. Anticipated additions: ~3 rows.
- **Browser-enforced Subresource Integrity for the executive deck.** Computing the SHA-384 hashes for the three pinned CDN URLs (reveal.js 5.1.0, Mermaid 11.10.0, Lucide 0.460.0) in an internet-enabled environment and adding `integrity="sha384-..."` attributes to the three CDN `<script>` tags activates browser-side SRI enforcement, completing the compensating-controls strategy documented in Row 21. The `crossorigin="anonymous"` attribute is already in place; no other HTML edit is required to activate the hash check. Anticipated additions: zero new decision rows because Row 21 already documents the SRI-enabling path; the addition is purely procedural.
- **Build-time cross-surface duration formatting parity assertion.** Adding `scripts/validate_consistency.py::check_duration_formatting_parity()` to assert that M4 and M7 phase values render identically across `acceleration-report.md` and `executive-presentation.html` would make the structural invariant documented in Row 22 machine-checkable. The assertion is redundant with the shared-helper invariant at this checkpoint but would catch any future surface-specific divergence introduced as a maintenance error. Anticipated additions: zero new decision rows because Row 22 already documents the parity intent; the addition is purely procedural.

Each anticipated category would push the table count from twenty-five toward roughly thirty-three if all four exploratory extensions were undertaken; the two procedural additions (browser-enforced SRI and parity assertion) require zero new rows because Rows 21 and 22 already document the corresponding intent. The Decisions table's row-number stability for the original seventeen rows, the QA-Checkpoint-1 follow-on rows (18, 19, and 20), the QA-Checkpoint-2 follow-on rows (21 and 22), and the QA-Checkpoint-B follow-on rows (23, 24, and 25) is preserved across all such additions; any reordering would be a separate, explicit decision recorded in its own row.

### Decision Provenance

Each decision in the table has a provenance: a user-stated instruction that mandates the choice, an exercise of agent latitude allowed by the user's "Agent Latitude" instruction, or a conflict resolution between two user-stated rules. The provenance map below allows a reader to verify that no decision was made arbitrarily; every choice has a documented origin.

- **User-stated instruction (eight rows).** Rows 2 (window boundary majority-of-days), 5 (reverts-of-reverts exclusion), 6 (release source fallback precedence), 7 (prerelease exclusion), 9 (M12 insufficient-signal fallback), 11 (Blitzy as engineering actor), 15 (per-module weighted aggregation), and 16 (UTC window boundaries) are mandated by explicit user instructions in the Agent Action Plan. The wording of the user's instruction is quoted in the corresponding row's Rationale cell where it directly anchors the choice.
- **Agent latitude exercised (seven rows).** Rows 1 (inflection detection method), 3 (Flow Distribution waterfall ordering), 4 (revert attribution algorithm), 8 (M10 fallback signal selection), 10 (bot exclusion list composition), 12 (engineering-actor selector implementation), and 17 (cache-by-default for GitHub API) are choices that the user explicitly allowed the agent to make under the "Agent Latitude" instruction ("The table above defines WHAT to measure, not HOW. You choose the extraction strategy for each metric based on available data sources"). Each row documents which alternatives were considered and why the chosen approach was preferred.
- **Conflict resolution between user-stated rules (three rows).** Rows 13 (Observability non-applicabilities), 14 (Onboarding co-location), and 20 (Visual Architecture diagram substitution for non-migration deliverable) resolve direct conflicts between two user-stated rules; the conflicts are documented in the Deviations section above. Each conflict-resolution row carries a corresponding deviation entry, and the two cross-reference each other (Row 13 ↔ Deviation 2, Row 14 ↔ Deviation 1, Row 20 ↔ Deviation 3).
- **QA Checkpoint feedback (seven rows).** Rows 18 (M6 placeholder aliasing rule) and 19 (pre-baked per-metric rows in Requirements Traceability Matrix) were appended in response to QA Checkpoint 1 findings (Info Issue #4 and Minor Issue #2 respectively). Row 18 makes an implicit aliasing rule explicit so the build-time consistency check can verify it; Row 19 documents the design choice to use pre-baked rows rather than a block-substitution placeholder, where the choice was previously undocumented. Rows 21 (CDN dependency pinning and SRI strategy) and 22 (cross-surface duration formatting policy) were appended in response to QA Checkpoint 2 findings. Row 21 resolves QA Checkpoint 2 Major findings on the Mermaid 11.4.0 vulnerability (build_presentation.py Finding 6) and on the SRI omission (decision-log.md finding on undocumented security decision); Row 22 resolves QA Checkpoint 2 Major finding on cross-surface formatting drift (build_presentation.py Finding 2). Rows 23 (`BLITZY_RUN_ID` validation constraints), 24 (Puppeteer infrastructure-failure reclassification and container-friendly sandbox override in `render_diagrams.py`), and 25 (HTML entity decoding for HTML-extracted Mermaid blocks) were appended in response to QA Checkpoint B findings. Row 23 resolves QA Checkpoint B Issue #2 (path-traversal vulnerability in `BLITZY_RUN_ID`) and Issue #3 (unhandled `OSError` on excessively long values); Row 24 resolves QA Checkpoint B Issue #1 (Puppeteer launch failures misclassified as Mermaid syntax errors in containerized environments); Row 25 documents the HTML entity decoding step that surfaced during the Row 24 verification and was added as part of the same fix to mirror browser-side `element.textContent` semantics. All seven rows record the conscious design decision rather than altering the underlying methodology, which is why none of them is a "user-stated instruction" or an "agent latitude exercised" choice in the strict sense; the QA Checkpoint surface is the third channel through which non-trivial decisions enter the log. Row 20 was also surfaced by QA Checkpoint 1 (Major Issue DL1) but its provenance is conflict resolution rather than QA-checkpoint feedback because the decision content resolves the conflict between the Visual Architecture rule and the non-migration scope.

The provenance counts add up to twenty-five (eight user-stated + seven agent-latitude + three conflict-resolution + seven QA-checkpoint-feedback). No decision is unaccounted for; the table is complete with respect to the implementation choices made during the initial harness run, the documentation choices made in response to QA Checkpoint 1, the security and consistency choices made in response to QA Checkpoint 2, and the security and infrastructure-classification choices made in response to QA Checkpoint B.

### Methodological Notes

The Decisions table records each non-trivial choice in compact form. The notes below expand on three methodological points that span multiple rows and that a reader may want to understand before consulting the table.

The first methodological note concerns the **identical-methodology guarantee** that the user mandated for Baseline and After periods. Three of the twenty-five rows (Row 2 window boundary handling, Row 12 engineering-actor selector, Row 16 UTC window boundaries) collectively realize this guarantee. The guarantee is not enforced by a runtime assertion; it is structural, in the sense that the extraction functions are parameterized over `(phase_name, date_range, actor_selector)` only and the selector itself contains exactly one branch on phase. Any future change that violated the guarantee would require modifying the same code path for both periods simultaneously, which is detectable by code review.

The second methodological note concerns **confidence assignment**. The user's instruction is that "a metric derived from direct counts in an issue tracker is High confidence. A metric approximated from git commit patterns is Medium confidence. A metric inferred from indirect proxies is Low confidence." This rule is implemented by the source-precedence chain in each per-metric extraction function (Rows 6, 8, 9 directly encode source-precedence fallbacks). The confidence tier is recorded in `data/metric_<N>.json#confidence` at extraction time and is the tier of the source actually used, not the tier of the source the metric "should" have. The dashboard's confidence-distribution pie chart and the report's per-metric caveat callouts read from this field.

The third methodological note concerns **the relationship between Metrics 3, 4, 5, and 7**. Metric 3 (Flow Predictability) is derived from Metric 2; Metric 5 (Flow Efficiency) is derived from Metrics 4 and 7; Metric 4 (Flow Active) and Metric 7 (Flow Time) are themselves independent extractions. The decision-log rows that affect M4 and M7 (Rows 12 engineering-actor selector and the not-explicitly-rowed history-rewrite-exclusion in M7) propagate to M5. The confidence tier of M3 is the same as M2's tier; the confidence tier of M5 is the minimum of M4's and M7's tiers. These propagation rules are implemented in `validate_consistency.py::check_derived_confidence()` and are not separately documented as decision rows because they are mechanical consequences of the user's instruction.

The fourth methodological note concerns **the boundary case where post-introduction history is shorter than 90 days**. The user instructed: "If fewer than 90 days of post-introduction data exist, report Baseline vs Post-Introduction only." `blitzy-cal` had 80 days of post-introduction history at the time of the AAP authoring (2026-02-25 inflection to 2026-05-15 cutoff); if the harness is re-run before 2026-05-25, the fallback applies. The harness computes `days_since_inflection = (cutoff_date - inflection_date).days` and selects the two-phase reporting mode when the value is less than 90. The selection is recorded in `data/windows.json#reporting_mode` (`three_phase` or `two_phase`); the report renderer reads this field and emits the appropriate section headings. This boundary handling is not a separate decision row because the user's instruction is unambiguous and the implementation is mechanical.

The fifth methodological note concerns **the treatment of merge commits in commit-volume calculations**. Per the user's instruction, per-module aggregation is weighted by "non-merge commits per module / total" (AAP §0.1.3). The harness uses `git log --no-merges --format='%H %s'` for commit-volume queries and `git log --merges` only for the merge-commit timestamps used by Metric 7. This consistent exclusion of merge commits from volume counts is applied across all per-module aggregations and is not a separate decision row because it follows directly from the user's instruction.

### Runtime Configuration Summary

The harness exposes a small number of command-line flags and environment variables that together control every non-data-source decision recorded in this log. The table below is a configuration reference for re-runs and for reversing a decision through the High-reversibility procedure.

| Configuration Setting | Type | Default | Affects Decisions | Effect |
|------------------------|------|---------|-------------------|--------|
| `GITHUB_TOKEN` | env var | required | Row 8 (M10 audit-log fallback) | Token scopes determine which M10 extraction path is used; `audit_log:read` enables the full-signal path. |
| `LINEAR_API_KEY` | env var | optional | Row 9 (M12 SLA-source fallback) | If present, M12 queries Linear; if absent, M12 falls back to repository-policy search or reports insufficient signal. |
| `BLITZY_RUN_ID` | env var | UUIDv4 at startup | (all rows; correlation ID) | Used as the directory name under `logs/<run_id>/` and as the correlation field in every log line. |
| `--method` | CLI flag on `derive_inflection.py` | `co-author` | Row 1 (inflection detection method) | Selects between author-identity, velocity, or both candidates for the inflection date. |
| `--tolerance-days` | CLI flag on `derive_inflection.py` | `30` | Row 1 (inflection reconciliation) | Tolerance window for reconciling the two candidate inflections. |
| `--boundary-method` | CLI flag on `generate_windows.py` | `majority` | Row 2 (window boundary handling) | Selects between `majority`, `split`, or `start` for boundary windows. |
| `--metric` | CLI flag on `extract_metrics.py` | `all` | (per-metric extractions) | Selects which metric (1..12 or `all`) to extract. |
| `--no-cache` | CLI flag on `extract_metrics.py` | `False` | Row 17 (cache-by-default) | Forces fresh fetches for the duration of one invocation; cache contents are otherwise preserved. |
| `BOT_LOGINS` | Python constant in `scripts/_shared.py` | `{dependabot[bot], github-actions[bot], renovate[bot], kodiak[bot]}` | Row 10 (bot exclusion) | Set of login strings filtered out of M1, M2, and per-actor breakdowns. |
| `TZ` | Python constant in `scripts/_shared.py` | `timezone.utc` | Row 16 (UTC window boundaries) | Timezone applied to every `datetime` arithmetic operation in the harness. |
| `LABEL_CATEGORY_MAP` | Python constant in `scripts/_shared.py` | documented map | Row 3 (Flow Distribution classification) | Maps GitHub Issue labels to the four flow-distribution categories. |
| `REQUIRED_MERMAID_VERSION` | Python constant in `scripts/build_presentation.py` | `"11.10.0"` | Row 21 (CDN dependency pinning) | Pinned Mermaid version enforced by `validate_cdn_versions()`; build fails if any CDN tag references a different version. |
| `format_duration_seconds`, `is_duration_seconds_metric` | Python helpers in `scripts/_shared.py::Section 17b` | canonical (no toggle) | Row 22 (cross-surface duration formatting) | Canonical formatter for metrics with `unit == "seconds"` (M4, M7); both renderers import these symbols so the same input always produces the same output across surfaces. |
| `VALID_RUN_ID_RE`, `MAX_RUN_ID_LENGTH`, `RESERVED_RUN_IDS` | Python constants in `scripts/_shared.py::Section 5b` | `re.compile(r"^[A-Za-z0-9._-]{1,64}$")`, `64`, and `frozenset({".", ".."})` | Row 23 (`BLITZY_RUN_ID` validation) | Two-stage validation applied to operator-supplied `BLITZY_RUN_ID` values inside `get_or_create_run_id()` before any filesystem operation. Stage one is the regex character-class allow-list and 64-character length cap; stage two is the reserved-name post-check that rejects the pure-dot strings `"."` and `".."` (which the regex would otherwise admit because the literal `.` is required for UUIDv4 and semver-style labels). Non-conforming values are rejected with a structured `ValueError` that names the variable, the observed length, the maximum allowed length, the allowed character class, and the regex pattern (or, for reserved-name failures, the specific filesystem-reserved category and the resulting resolution outside the intended scope); the offending value itself is deliberately not echoed, to prevent log injection through the error-handling path. |
| `_INFRASTRUCTURE_FAILURE_SIGNATURES`, `_PUPPETEER_LAUNCH_ARGS` | Python constants in `scripts/render_diagrams.py` | 26-entry substring tuple; `("--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage")` | Row 24 (Puppeteer infrastructure-failure reclassification and container-friendly sandbox override) | The signature tuple is consulted by `_classify_cli_failure()` to distinguish infrastructure failures from genuine syntax errors; on infrastructure classification the `cli_syntax_check()` raises `CLIUnavailable` and the existing fallback path uses the regex validator. The Puppeteer-args tuple is emitted into a per-process JSON config and threaded through `mmdc` via `--puppeteerConfigFile` to preempt container-blocked sandbox launches. The `--no-cli` CLI flag on `render_diagrams.py` is the explicit operator escape hatch. |
| `html.unescape` call inside `extract_mermaid_blocks_from_html()` | Python stdlib function in `scripts/render_diagrams.py` | applied unconditionally | Row 25 (HTML entity decoding) | Decodes HTML entities in extracted `<pre class="mermaid">` block bodies so the standalone Mermaid CLI sees the same source that `element.textContent` exposes to the browser-side Mermaid library at deck-render time. The Markdown extractor does NOT apply this decoding because Markdown fenced code blocks are parsed verbatim. |

Every configuration setting in the table above has a corresponding decision row in the Decisions table; conversely, every High-reversibility decision row has at least one configuration setting that exposes the reversal. The Medium- and Low-reversibility decisions do not have configuration settings because their reversal requires code edits or constraint relaxations rather than runtime toggles, except Row 22 which exposes the canonical helper as an importable symbol (the helper itself is the configuration surface; replacing its body changes formatting behavior across both renderers in one edit). Rows 23, 24, and 25 expose their controls as importable constants and helper functions rather than as CLI flags or environment variables; this is intentional because the constraints they encode are defense-in-depth controls (Row 23), infrastructure detection logic (Row 24), and source-language semantics (Row 25), each of which is better expressed as code than as a runtime toggle. Row 24 additionally exposes the `--no-cli` CLI flag as the explicit operator escape hatch for environments where the regex validator is preferred regardless of CLI availability.

### Glossary of Terms

The glossary below defines terms used in the Decisions table that may be unfamiliar to readers who have not consulted the Agent Action Plan or the main analytical report. Terms are listed in alphabetical order to assist lookup.

- **Agent Latitude.** The user's instruction that "the table above defines WHAT to measure, not HOW; you choose the extraction strategy for each metric based on available data sources." Decisions made under this latitude are tagged as such in the Decision Provenance section.
- **Baseline phase.** The temporal period before the AI tool introduction date (`9d80a5d026` dated 2026-02-25T00:24:31Z for `blitzy-cal`). All commits, PRs, reviews, and issues authored before this date are assigned to the Baseline phase.
- **Confidence tier.** One of High, Medium, or Low; assigned per metric based on the data source actually used at run time. The tier is recorded in `data/metric_<N>.json#confidence` and surfaces in the dashboard and the report's per-metric callouts.
- **Engineering actor.** The entity producing code on a PR. In the Baseline phase, the engineering actor is the human author; in the After phase, the engineering actor is Blitzy Agent for Blitzy-authored PRs and the human author for other PRs. The selector function `engineering_actor(pr, phase)` encodes this choice.
- **Flow Active.** The sum of the engineering actor's coding span durations across the working phases of a PR. Working phases are bounded by review events; gaps within a span are not subtracted.
- **Flow Efficiency.** The ratio `Flow Active / Flow Time` per PR; the median ratio across PRs in a phase is the metric value.
- **Flow Load.** The mean count of in-progress PRs (open OR draft with at least one commit) at the end of each Monday-aligned two-week window. Bots are excluded; Blitzy Agent is not a bot and is included.
- **Flow Predictability.** The reciprocal of the coefficient of variation (`mean / stdev`) of Flow Velocity across windows in a phase. Phases with fewer than four windows or zero variance report "Insufficient signal" rather than a numeric value.
- **Flow Time.** The wall-clock duration from the first commit on the PR branch to the merge commit on the default branch.
- **Flow Velocity.** The count of PRs merged to the default branch per Monday-aligned two-week window.
- **Inflection date.** The date at which the AI tool was introduced to the repository, derived per Row 1. The inflection date partitions the analysis window into Baseline and After phases.
- **Insufficient signal.** A metric status emitted when the data source required for a metric is unavailable, ambiguous, or below the threshold required for a meaningful computation. The status is paired with a `reason` field that names the source attempted.
- **Monday-aligned window.** A two-week interval starting on a Monday at 00:00:00 UTC and ending on the following Sunday at 23:59:59 UTC. Windows are generated by snapping the inflection date backward to the most recent Monday and emitting intervals both forward and backward.
- **Ramp-Up phase.** The first 90 days after the AI tool introduction date. Defined by the user as one of three temporal phases (Baseline, Ramp-Up, Steady State).
- **Reverts-of-reverts.** Revert commits whose original commit is itself a revert. These are excluded from Metric 8 per user instruction because they represent re-applications of previously reverted changes, not new defect indicators.
- **Steady State phase.** The period beyond 90 days after the AI tool introduction date. If fewer than 90 days of post-introduction data exist at run time, the harness reports Baseline vs. Post-Introduction instead of Baseline vs. Ramp-Up vs. Steady State.
- **Unattributable revert.** A revert commit whose original commit cannot be identified by either the body-line parse or the tree-hash lookup. Counted as a separate sub-count of Metric 8.

### Rule Grounding Matrix

The user enumerated eleven rules in the project setup (five Specified Rules: Observability, Onboarding & Continued Development, Explainability, Visual Architecture Documentation, Executive Presentation) plus six task-framework rules (Rule 1 Data Provenance, Rule 2 Factual-Neutral Tone, Rule 3 Confidence Transparency, Rule 4 Internal Consistency, Rule 5 Reproducibility, Rule 6 Environment First). Every rule maps to at least one decision in the table above; the matrix below records the mapping so a reviewer can trace any rule to the decisions that operationalize it.

| User Rule | Decisions That Implement It | Surfaces Where Compliance Is Visible |
|-----------|------------------------------|--------------------------------------|
| Observability | Row 13 (non-applicabilities), structured logging in every script; Row 24 (structured logging of CLI classification results and Puppeteer config emission) | `logs/<run_id>/` directories; `dashboard.md`; README Observability section; `logs/<run_id>/render_diagrams.log` with `failure_class` and `fallback_validator` context fields |
| Onboarding & Continued Development | Row 14 (co-location); Row 23 (Common Pitfalls and Environment Variables Reference updated for `BLITZY_RUN_ID` validation); Row 24 (Common Pitfalls entry for container-friendly `--no-cli` invocation) | `blitzy/reports/acceleration/README.md` |
| Explainability | The decision log itself; every row's Rationale cell; Row 21 (security-decision documentation for SRI omission); Row 23 (defense-in-depth documentation for `BLITZY_RUN_ID` validation); Row 24 (infrastructure-vs-syntax classification documentation); Row 25 (browser-parity decoding documentation) | This file; the Audit Trail subsection |
| Visual Architecture Documentation | Deviation 3 (temporal-phase diagram substitution); Row 25 (HTML entity decoding preserves visual-architecture diagrams' validity in the executive deck) | Methodology section of `acceleration-report.md`; Mermaid diagrams in the executive deck |
| Executive Presentation | Row 21 (CDN dependency pinning with SRI compensating controls); reveal.js 5.1.0 pinning; Row 24 (CLI validator no longer false-positives on container environments, so the validator can be run as part of the deck's build pipeline without misclassifying its diagrams); Row 25 (HTML entity decoding so the deck's diagrams are validated against the same source the browser parses) | `executive-presentation.html` with pinned CDN versions and `crossorigin` attributes |
| Rule 1 — Data Provenance | Rows 4, 6, 8, 9, 17 (source-precedence and caching) | Requirements Traceability Matrix in `acceleration-report.md` |
| Rule 2 — Factual-Neutral Tone | (style convention; not a row) | `build_report.py` grep pass for subjective qualifiers |
| Rule 3 — Confidence Transparency | Rows 6, 8, 9 (confidence downgrades on fallback) | Per-metric caveat callouts in `acceleration-report.md` |
| Rule 4 — Internal Consistency | Row 12 (single selector function); Row 22 (cross-surface duration formatting policy); Row 25 (HTML entity decoding so the CLI validator and the browser see the same source); `validate_consistency.py` | `data/consistency_report.json`; identical M4 and M7 phase values in `acceleration-report.md` and `executive-presentation.html` |
| Rule 5 — Reproducibility | Row 17 (cache-by-default); Row 23 (`BLITZY_RUN_ID` validation produces deterministic correlation-ID handling across re-runs because invalid values are rejected uniformly); `commands.log` | Reproducibility Appendix in `acceleration-report.md` |
| Rule 6 — Environment First | (procedural; `verify_environment.py` is the first script); Row 23 (`BLITZY_RUN_ID` validation runs before any filesystem operation and before any other script in the pipeline) | Environment Verification section of `acceleration-report.md` |

The matrix shows that every user-stated rule is operationalized either by a decision in the table or by a procedural artifact (structured logging, the consistency validator, the commands log) whose existence is itself a consequence of the choices documented here. No rule is left without a corresponding implementation; conversely, every decision row contributes to at least one rule's satisfaction. Row 21 contributes to the Executive Presentation rule (the deck remains production-deployable through the version upgrade and compensating controls) and to the Explainability rule (the SRI omission is now an explicit decision rather than a silent gap); Row 22 contributes to Rule 4 Internal Consistency (the same metric value renders identically across the Markdown report and the HTML deck); Row 23 contributes to the Explainability rule (the defense-in-depth `BLITZY_RUN_ID` validation is now an explicit decision rather than an absent control), to the Onboarding rule (the validation constraint is documented in the README's Common Pitfalls and Environment Variables Reference), and to Rule 6 Environment First (the validation runs before any filesystem operation); Row 24 contributes to the Observability rule (the structured logging now records `failure_class` and `fallback_validator` context fields so operator logs distinguish infrastructure from syntax errors), to the Explainability rule (the classification logic is now an explicit decision), and to the Onboarding rule (the `--no-cli` escape hatch is documented in the README's Common Pitfalls); Row 25 contributes to the Visual Architecture rule (the executive deck's Mermaid diagrams are validated against the same source the browser parses) and to Rule 4 Internal Consistency (the CLI validator and the browser-side renderer agree on the diagram source after decoding).

### Decision Validation Procedures

A reviewer who wants to verify that this decision log is internally consistent and matches the harness implementation can apply the procedures listed below. Each procedure is a concrete command or a concrete file inspection that returns a definite yes/no answer.

1. **Row count check.** Run `grep -c '^| [0-9]' blitzy/reports/acceleration/decision-log.md` and confirm the result is at least twenty-five (seventeen original mandatory rows plus the three QA-Checkpoint-1 follow-on rows plus the two QA-Checkpoint-2 follow-on rows plus the three QA-Checkpoint-B follow-on rows). If new decisions have been appended after these, the count will be higher.
2. **Reversibility tag check.** Run `grep -oP '\| (High|Medium|Low) —' blitzy/reports/acceleration/decision-log.md | sort | uniq -c` and confirm the totals are High=19, Medium=3, Low=3 (or higher with appended rows; the mandatory floor is 19/3/3 after the Checkpoint B follow-on additions).
3. **Subjective qualifier check.** Run `grep -iE 'impressive|significant|excellent|remarkable|unfortunately|notable|striking|dramatic' blitzy/reports/acceleration/decision-log.md` and confirm zero matches.
4. **Emoji check.** Run `python3 -c 'import re; print(len(re.findall(r"[\U0001F300-\U0001F9FF\u2600-\u27BF\U0001F600-\U0001F64F]", open("blitzy/reports/acceleration/decision-log.md").read())))'` and confirm the result is zero.
5. **Cross-reference check.** Verify that each "See Decision Row N" reference in `acceleration-report.md`, `dashboard.md`, `executive-presentation.html`, and `README.md` resolves to a row in the Decisions table whose topic matches the cited content.
6. **Implementation Anchor freshness check.** For each row in the Implementation Anchors subsection, verify that the named file exists under `scripts/` (or under `blitzy/reports/acceleration/` for the documentation-only anchors) and contains the named function or constant.
7. **Risk Mitigation Index check.** For each row in the Risk Mitigation Index, verify that the named mitigation site exists and is consulted by the corresponding metric's extraction code or by the consistency validator.
8. **Deviation completeness check.** Confirm that the Deviations section lists exactly three deviations, each cross-referenced to a Decisions-table row.
9. **Provenance balance check.** Verify that the Decision Provenance counts (user-stated + agent-latitude + conflict-resolution) sum to the total row count in the Decisions table.

Failures of any of the above procedures indicate either a defect in the decision log or a drift between the log and the implementation; both are correctable by editing the log, the implementation, or both, with the change recorded in the Audit Trail.

### Limitations of This Log

The decision log records every non-trivial implementation choice but does not capture three categories of information that a reader should be aware of when consulting it.

- **Trivial decisions are not recorded.** Choices that any competent engineer would have made the same way are not in the table; the Explainability rule's "competent engineer could reasonably have chosen differently" criterion defines the inclusion threshold. Examples of trivial choices that are deliberately omitted: the choice of Python 3 stdlib `logging` over a third-party logger; the choice to write JSON output rather than YAML; the choice to use ISO 8601 timestamps. If a reviewer believes any of these is non-trivial, the row can be added with rationale.
- **Counterfactual analyses are not exhaustive.** The Alternatives Considered cells enumerate the alternatives that were evaluated during implementation, not every conceivable alternative. A reviewer who wants to argue for an unconsidered alternative should add their argument as a new row in the Audit Trail rather than retroactively editing the Alternatives Considered cell.
- **Implementation-internal decisions are not in the log.** Choices about variable naming, function decomposition, code structure within a single function, and similar internal matters are recorded in the code itself (and in the docstrings) rather than in this log. The log is the source of truth for "why" decisions but not for "how the code is organized within a file"; the latter is governed by `scripts/_shared.py`'s structure and the per-script docstrings.

The three limitations above are intentional, not accidental. The decision log is a focused artifact: its purpose is to record the choices that distinguish this implementation from alternative implementations a different agent could have produced. The log is not a comprehensive design document.

### Closing Note

This decision log is the single source of truth for the "why" decisions behind the Development Acceleration Measurement. The Decisions table above is the primary surface; the Deviations, Reversibility Summary, Implementation Anchors, Risk Mitigation Index, Audit Trail, Future Watchpoints, Decision Provenance, Methodological Notes, Runtime Configuration Summary, Glossary, and Decision Validation Procedures subsections are supporting surfaces that elaborate specific aspects of the same set of choices. A reader who reads only the Decisions table can understand every non-trivial choice that was made; a reader who reads the full log can additionally locate the implementation of each choice, verify the mitigation of each risk, audit the review history, anticipate where new decisions may arise in future re-runs, trace each decision to its origin in user instructions or agent latitude, look up definitions for the technical terms used, and validate the log's internal consistency through the documented procedures.

The log will be updated as the harness is re-run against new windows of `blitzy-cal` history or against other repositories in the Blitzy-Sandbox organization. The twenty-five recorded rows are immutable in content and position (the first seventeen from the AAP, the next three appended in response to QA Checkpoint 1, Rows 21 and 22 appended in response to QA Checkpoint 2, and Rows 23, 24, and 25 appended in response to QA Checkpoint B); additions are appended after Row 25 with their own provenance and reversibility detail. Any future amendment to an existing row is recorded as a separate "Decision Amendment" entry in the Audit Trail with the original row text and the amendment text preserved together so the history is fully auditable. The log's purpose is not to justify the implementation after the fact but to record the rationale that drove the implementation; the order of authorship (decision recorded before implementation written) is the structural enforcement of that purpose, and the Explainability rule's prohibition on embedding rationale in code comments is its structural prerequisite.

### Quick Reference Card

The card below is a one-screen summary for readers who need to locate a specific surface quickly without navigating the full log. It is the last section of the document so that scrolling to the end provides immediate orientation.

| Surface | Location | Purpose |
|---------|----------|---------|
| Decisions table | `## Decisions` (above) | Primary record of all twenty-five non-trivial choices (seventeen original + three QA-Checkpoint-1 follow-ons + two QA-Checkpoint-2 follow-ons + three QA-Checkpoint-B follow-ons) |
| Implementation Anchors | `### Implementation Anchors` | Maps each decision to its code location |
| Risk Mitigation Index | `### Risk Mitigation Index` | Verifies each risk has an implemented mitigation |
| Deviations | `## Deviations from Literal Interpretation` | Records the three rule-conflict resolutions |
| Resolution Verification | `### Resolution Verification` | Procedures to verify each deviation's resolution |
| Reversibility Summary | `## Reversibility Summary` | Categorizes decisions by reversal difficulty |
| Per-Decision Reversibility Detail | `### Per-Decision Reversibility Detail` | Names the concrete reversal procedure for each row |
| Audit Trail | `### Audit Trail` | Records the review checkpoints and amendment discipline |
| Worked Example | `### Worked Example — Row 1 Decision Flow` | Demonstrates the eight surfaces of a single decision |
| Future Watchpoints | `### Future Watchpoints` | Anticipates where new decisions may arise in re-runs |
| Decision Provenance | `### Decision Provenance` | Traces each decision to its origin |
| Methodological Notes | `### Methodological Notes` | Cross-row methodology details |
| Runtime Configuration Summary | `### Runtime Configuration Summary` | Flags and environment variables that affect decisions |
| Glossary | `### Glossary of Terms` | Definitions of technical terms used in the log |
| Rule Grounding Matrix | `### Rule Grounding Matrix` | Maps each user-stated rule to the decisions that implement it |
| Decision Validation Procedures | `### Decision Validation Procedures` | Machine-checkable rules for log correctness |
| Limitations | `### Limitations of This Log` | What the log does and does not capture |
| Style Conventions | `### Style Conventions` | Formatting rules for the log itself |
| Suggested Reading Order | `### Suggested Reading Order` | Entry points for different reader profiles |

The card lists every subsection in the log so a reader can navigate by Ctrl-F to any surface without scrolling. The locations are stable; if a future amendment adds a new subsection, the card will be updated to reflect the addition.

