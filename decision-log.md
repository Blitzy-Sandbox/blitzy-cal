# Decision Log — Snyk Security Scan (Config H)

## Preamble

This document is the rule-mandated **decision log** for the Snyk Security Scan (Config H) exercise executed against the `blitzy-cal` (Cal.com monorepo) codebase. The source of every decision recorded below is the Agent Action Plan (AAP) for Config H, which encodes the user's four CRITICAL directives — (1) install and authenticate the Snyk CLI via `SNYK_TOKEN`, (2) execute the Snyk SAST scan (`snyk code test`) and capture its SARIF output, (3) execute the Snyk SCA scan (`snyk test`) and capture its JSON output, and (4) normalize and merge the findings into a single-line minified JSON artifact named `findings-config-h.json` conforming to a 5-field-per-finding schema (`file`, `line`, `severity`, `cwe`, `description`). The artifact you are reading is required by the user-specified **Explainability rule** (AAP §0.7.1.1), which mandates that every non-trivial implementation decision be documented as a Markdown table of "what was decided, what alternatives existed, why this choice was made, and what risks it carries" and that every deviation from a literal or obvious interpretation of the requirements have an explicit entry. A **bidirectional traceability matrix is NOT applicable** to this exercise because this is not a migration or refactor — no source-construct-to-target-implementation mapping exists; per AAP §0.6.2 the decision-table form alone is the deliverable. This document is the **SINGLE SOURCE OF TRUTH** for every "why" decision across the Config H exercise: per the Explainability rule, rationale is NOT embedded in code comments in `findings-config-h.json`, `executive-presentation.html`, or `scripts/normalize-snyk-findings.mjs`; this log is the only place where rationale lives.

## Scope of Decisions Captured

This log enumerates decisions across five surfaces of the Config H exercise: (a) Snyk CLI installation, authentication, and invocation; (b) SAST scan (`snyk code test`) execution and SARIF parsing; (c) SCA scan (`snyk test`) execution and Snyk JSON parsing; (d) normalization, merging, severity mapping, prefix tagging, truncation, and serialization to `findings-config-h.json`; and (e) governance artifacts (this `decision-log.md` and the rule-mandated `executive-presentation.html`). Decisions internal to the executive presentation's visual identity, slide ordering, or CDN pinning are NOT duplicated here — those are dictated by the Executive Presentation rule (AAP §0.7.1.2) and are non-trivial only insofar as the rule itself constrains them; the rule text governs.

## Decision Table

| Decision | Alternatives | Rationale | Risks |
| --- | --- | --- | --- |
| **1. Correct the user's literal shell syntax for the SCA scan.** Interpret `snyk test --json > results-snyk-deps.json /path/to/blitzy-cal` as `cd /path/to/blitzy-cal && snyk test --all-projects --json > results-snyk-deps.json`. | (a) Run the literal command as written, leaving `snyk` to scan whatever directory it is invoked from and treating `/path/to/blitzy-cal` as a positional `snyk test` argument; (b) error out and surface the literal form to the operator for clarification. | In POSIX shells the redirection operator and its target (`> results-snyk-deps.json`) are stripped from the argument vector before the command runs, so `/path/to/blitzy-cal` does survive as a positional argument to `snyk test`. The `cd && …` rewrite is preferred not because POSIX would drop the path token, but because it pins the scan's working directory deterministically (so `--all-projects` traverses the intended monorepo root), keeps relative manifest paths in the SCA output anchored at the repo root rather than at an absolute prefix, pairs cleanly with the `--all-projects` flag added in row 2, and eliminates ambiguity about whether `snyk test` honors a trailing positional path on every CLI minor (behavior has historically varied across Snyk CLI releases). The corrected form preserves the user's intent — scan the `blitzy-cal` working tree and write output to `results-snyk-deps.json`. | User may have intended the literal positional form for explicit out-of-tree invocation; mitigated by documenting the deviation here and by validating the resulting artifact contains a `vulnerabilities` array. |
| **2. Append `--all-projects` to the SCA scan invocation.** Final form: `snyk test --all-projects --json > results-snyk-deps.json`. | Bare `snyk test` (scans only the root `package.json` + `yarn.lock`); `snyk test --yarn-workspaces` (Yarn 1 classic workspace mode — not applicable to Yarn 4); pre-build a list of manifests and loop `snyk test <manifest>` per manifest. | The repository is a Yarn 4 workspace monorepo with 119 `package.json` manifests across `apps/*`, `apps/api/*`, `packages/*`, `packages/embeds/*`, `packages/features/*`, `packages/app-store`, `packages/app-store/*`, `packages/platform/*`, `packages/platform/examples/base`, and `example-apps/*` (per the root `package.json` workspaces declaration). Without `--all-projects`, Snyk scans only the root and misses the 118 nested manifests, yielding a non-comprehensive SCA picture. | `--all-projects` may surface duplicate vulnerabilities across workspaces (the same transitive package depended on by multiple workspaces is reported once per workspace); acceptable because the user's schema does not require deduplication and downstream comparison harnesses can deduplicate if needed. |
| **3. Produce two rule-mandated files beyond the headline "1 new file" framing.** Author `decision-log.md` (this file) and `executive-presentation.html` in addition to `findings-config-h.json`. | Limit deliverables to the headline-stated single file; omit the rule-mandated artifacts; bundle the explanations into `findings-config-h.json` as a comment (not valid JSON) or as an inline `__meta` field (violates the 5-field schema). | User-specified rules (Explainability rule + Executive Presentation rule, AAP §0.7) explicitly mandate these artifacts independently of any other documentation. Per the AAP rule-driven scope protocol, rule-mandated files extend scope beyond a headline framing; omitting them is treated as a defect. | Slight increase in deliverable file count vs. the headline framing; mitigated by clarifying scope in this very entry and by keeping the new files non-invasive (no project source modification, no manifest changes). |
| **4. Author a dedicated normalization script vs. inline `node -e`.** Create `scripts/normalize-snyk-findings.mjs` as a Node.js ESM module. | Inline `node -e "..."` invocation embedded directly in the shell command stream; bash + `jq` pipeline; Python one-liner with `json` module. | Auditability per the Explainability rule — a separate, reviewable, self-documenting script is required so the normalization algorithm is inspectable in version control. `jq` is not installed in the execution environment (confirmed during discovery via `which jq` returning nothing); Python is available but introduces an unnecessary language switch when Node.js is already required for the rest of the toolchain. | Adds one file to the working-tree footprint; does not introduce any third-party dependency because the script uses only Node.js built-ins (`fs`, `path`, `process`). |
| **5. Default to `"low"` for unmapped SARIF severity levels.** When SARIF `level` is missing or is `"none"` (or any value other than `error` / `warning` / `note`), emit `severity: "low"`. | Error and abort the scan (over-strict for an exploratory comparison harness); emit an empty string (violates the schema's `<critical\|high\|medium\|low>` value set); default to `"medium"` (over-amplifies the signal); default to `"high"` (similarly over-amplifying). | The user's mapping table only covers SARIF levels `error` (→ critical), `warning` (→ high), and `note` (→ medium). SARIF v2.1.0 also permits `level: "none"` and absent-level results; a defensive default of `"low"` keeps the output strictly within the user's value set without introducing a sixth severity bucket. | SARIF `level=none` findings (informational hints, not security issues) end up tagged `"low"` in the output, which slightly inflates the low-severity count; acceptable because all Config H sibling configs apply the same defensive rule, so the bias is uniform across the comparison harness. |
| **6. Use CWE first, CVE as fallback for the `cwe` field of dependency findings.** Compute `cwe = v.identifiers?.CWE?.[0] ?? v.identifiers?.CVE?.[0] ?? ""`. | Emit empty string `""` whenever CWE is absent (loses a usable identifier); emit the Snyk vulnerability ID (`SNYK-JS-PACKAGE-NNNNN`) which is Snyk-proprietary and not portable across tools; emit a serialized list of all identifiers (violates the schema's single-string field). | The user's mapping for SCA findings says "CVE ID; use CWE mapping if available" — interpreted as the standard Snyk identifier hierarchy: CWE is the most useful security taxonomy when present, CVE is the universal identifier and is always present when CWE isn't, and an empty string is the only neutral fallback. This matches the algorithm spelled out in AAP §0.5.4. | Mixed CWE-format and CVE-format strings in the same `cwe` column may confuse consumers expecting one uniform format; mitigated by documenting that the field carries an opaque identifier string whose format is one of `CWE-NNN`, `CVE-YYYY-NNNN`, or `""`. |
| **7. Emit empty string `""` for completely missing CWE and CVE.** When both `identifiers.CWE[0]` and `identifiers.CVE[0]` are absent on a dependency finding, write `cwe: ""`. | Omit the row entirely (loses the finding); emit `"none"` / `"UNKNOWN"` (introduces a sentinel value that consumers must special-case); emit `null` (violates the schema's implicit string typing because the user's example shape shows `"<CWE-ID>"` as a quoted string placeholder). | AAP Directive 4's pass/fail criterion is "every finding has all 5 fields populated" — a present key with an empty-string value satisfies "populated" (the key is present). Emitting `null` would break consumers that assume string typing for the `cwe` field. | Consumers must handle empty strings explicitly when grouping findings by CWE; documented as a schema note, not a defect. |
| **8. Use UTF-16 code-unit truncation (`String.prototype.slice(0, 200)`) for the `description` field.** Apply slicing AFTER prefix concatenation so the entire description (prefix + body) is bounded. | Byte-based truncation via `Buffer.byteLength` (more conservative for non-ASCII inputs); grapheme-cluster-aware truncation via `Intl.Segmenter` (correct for emoji/combining marks but slower and not needed for Snyk's English-language output); word-boundary truncation (could exceed 200 chars). | Snyk Code and Snyk Open Source descriptions are predominantly BMP ASCII English. For that input domain, UTF-16 code units map 1:1 to characters, so `slice(0, 200)` satisfies the user's "200 chars" criterion mechanically. Truncation is applied AFTER prepending `[snyk-code] ` / `[snyk-deps] ` so the prefix is preserved when long messages are clipped. | Non-BMP code points (rare in Snyk output but possible in vulnerable-package names or vendor messages) could be split mid-surrogate; extremely unlikely in practice and would manifest only as a single replacement-character glyph in the affected finding. |
| **9. Do NOT sort the merged findings array.** Preserve emission order: all SAST findings first (in the order SARIF emits them), then all SCA findings (in the order Snyk emits them, including per-project order under `--all-projects`). | Sort by severity descending (`critical > high > medium > low`); sort alphabetically by `file`; sort by Snyk-internal ID; deduplicate by `(file, line, cwe)` tuple. | The user's 5-field schema does not specify ordering, and the Pass/Fail criteria do not test ordering. Preserving insertion order keeps cross-Config diffs minimally noisy when sibling tool outputs are compared with `diff` or `git diff --no-index`. Sorting would mask emission-order changes from one Snyk CLI release to the next. | Order may differ from human-expected severity-descending presentation; acceptable for a mechanical comparison harness. Consumers that want a sorted view can `jq 'sort_by(.severity)'` post-hoc. |
| **10. Preserve the existing `.snyk` policy file as-is.** Do not modify, delete, or supplement `apps/api/v2/.snyk` (the only `.snyk` file in the repository, version `v1.25.1`, containing one `patch` entry for `npm:semver:20150403`). | Remove the file to obtain the "unfiltered" set of findings; copy the patch entry to a new root-level `.snyk` to apply it monorepo-wide; add an `ignore` entry for additional advisories. | The user explicitly preserved scope to "scan and report" (AAP §0.3.2 marks all `.snyk` and source code as read-only). Snyk honors the file automatically during the dependency scan when invoked from a tree containing it; modifying it would change the scan outcome and is outside the four directives. | One known semver advisory (`npm:semver:20150403`) may be legitimately filtered from the SCA output; this is the intended behavior of the existing policy and represents the project's accepted risk posture, not a defect in this exercise. |
| **11. Do NOT invoke `snyk monitor`.** Skip the dashboard-push entirely. | Run `snyk monitor --all-projects` to push the dependency graph to Snyk's dashboard for ongoing monitoring; run `snyk monitor` then defer the test to the cloud-side scheduler. | The four directives are scan + normalize, not push-to-dashboard. AAP §0.3.2 explicitly marks `snyk monitor` as out of scope. The exercise is a one-shot, local invocation feeding a downstream comparison harness, not an ongoing-monitoring setup. | No baseline tracking in Snyk's UI for trend analysis; not a goal for this exercise and easily added later if needed. |
| **12. Do NOT add a CI workflow.** No new file under `.github/workflows/`; no `snyk/actions` reference; no edits to existing `package.json` scripts. | Add `.github/workflows/snyk.yml` invoking the official `snyk/actions/node` action on pull-request and schedule triggers; add an npm script `"snyk:scan": "snyk test --all-projects --json"`. | One-shot local invocation per AAP §0.8.2. None of the existing 59 GitHub workflow files reference Snyk, SAST, CodeQL, or Dependabot; adding one would be a scope expansion beyond the four directives. The scan is reproducible from the four directives plus this log alone. | The scan is not automatically re-run on PRs; the comparison harness across configs may need periodic manual re-runs; acceptable for a one-time evaluation. |
| **13. Place ephemeral scan artifacts in the working tree, then delete after normalization.** Write `results-snyk-code.sarif` and `results-snyk-deps.json` to the repository root, consume them via the normalizer, then `rm` both files. | Write to `/tmp/snyk-config-h/`; pipe Snyk output via stdin into the normalizer; keep the artifacts permanently and add a `.gitignore` entry such as `results-snyk-*.{sarif,json}`. | Snyk CLI's `--sarif-file-output` switch writes to a relative-path file; piping SARIF via stdin works for `snyk test --json` but not symmetrically for `snyk code test`, so the working-tree placement is the path of least resistance for both. Deleting after normalization avoids modifying `.gitignore`, which would violate the AAP §0.4.2 "no application-level dependency or configuration changes" boundary. | Brief presence of ephemeral artifacts in the working tree (race window with concurrent `git add -A`); mitigated by deleting them as the final step before commit. |
| **14. Use `displayTargetFile` with `targetFile` fallback for SCA `file` field.** Compute `file = project.displayTargetFile ?? project.targetFile ?? ""` for each Snyk SCA finding. | Use `from[0]` (the top-level package name, not a manifest path); serialize the full `from[]` chain (multi-element string violates the single-field schema); use `project.packageManager + ":" + project.targetFile` (introduces a sentinel format). | The user's mapping says "Dependency manifest path (relative)". `displayTargetFile` is the field Snyk emits in modern CLI versions (1.1000+) carrying the human-readable relative manifest path; `targetFile` is the legacy alias still present in older releases. The `??` chain handles both. | Some Snyk output variants in edge cases (e.g., locked dependency aliasing) may lack both fields; fallback to `""` per row 7 keeps the schema valid. |
| **15. Use only the first `locations[]` entry per SARIF result.** Compute `file` and `line` from `result.locations?.[0]?.physicalLocation` for SAST findings. | Emit one finding row per `locations[]` element (multiplies the row count, breaks per-rule deduplication assumptions in the comparison harness); concatenate all locations into a single delimited string (violates the single-field schema). | Snyk Code's UI presents the first (sink) location as the canonical finding location. The user's 5-field schema has a single `file` field and a single `line` field per finding object, so a per-location explosion is not representable. Multi-location findings (e.g., taint flows with source + sink) are common in Snyk Code but the user's schema collapses them. | Multi-location findings lose their source/intermediate trace in the normalized output; acceptable because the full taint trace remains in the raw `results-snyk-code.sarif` for engineers needing depth. |
| **16. Append a single trailing newline to `findings-config-h.json` so that `wc -l` returns `1`.** Emit `<minified JSON>\n` (one LF byte). | Emit no trailing newline (technically still one line of content, but `wc -l` counts newline characters and would report `0`); emit `\r\n` (Windows line ending — would still count as one line on Linux `wc` but is non-canonical for the working environment). | The user's Pass/Fail criterion for Directive 4 is literal: `cat findings-config-h.json \| wc -l` must return `1`. POSIX `wc -l` counts trailing newlines, not "lines of content"; a file containing just `[]` with no newline reports `0`. Emitting `[]\n` (or `[{...}]\n`) satisfies the literal-by-literal interpretation. | Strict JSON-only consumers that reject any whitespace outside the JSON value would balk; the JSON specification (RFC 8259 §2) permits insignificant whitespace before and after the top-level value, so RFC-compliant consumers tolerate a trailing LF. |
| **17. Authenticate via `SNYK_TOKEN` environment variable, not `snyk config set`.** Export `SNYK_TOKEN` in the shell session before invoking any `snyk` subcommand. | Run `snyk auth <token>` (interactive browser flow — not headless-friendly); run `snyk config set api=<token>` (persists the token to `~/.config/configstore/snyk.json` on disk, increasing exposure surface). | Directive 1 specifies "authenticate via `SNYK_TOKEN` environment variable" verbatim. Environment-variable form is ephemeral, scoped to the shell session, and not persisted to disk, which is the least-privilege option for credential handling. | Token must be re-set per shell session; intentional and aligned with secret-management best practice. If `SNYK_TOKEN` is unset, the entire pipeline fails fast at Directive 1's `snyk auth check` validation — this is the desired failure mode. |
| **18. Use only built-in Node.js modules in the normalization script.** `scripts/normalize-snyk-findings.mjs` imports `node:fs`, `node:path`, and `node:process` — no third-party packages. | Use `jq` (not installed in the execution environment); add `lodash` / `fast-json-parse` / `ajv` to the workspace's `devDependencies` (would modify `package.json` and `yarn.lock`); shell-out to `cat` + `sed` + `awk`. | Adding a dependency violates AAP §0.4.2 "no application-level dependency changes" — the `package.json` and `yarn.lock` files must remain unchanged. Node.js built-ins (`fs.readFileSync`, `JSON.parse`, `JSON.stringify`, `fs.writeFileSync`) are sufficient for the manipulation required. | Slightly more verbose code than a `jq`-based one-liner would be; acceptable for the reviewability gain of an in-tree script. |
| **19. Emit object keys in the schema order `file, line, severity, cwe, description`.** Construct each finding literal as `{ file, line, severity, cwe, description }` in that order so `JSON.stringify` preserves it. | Alphabetical key order (`cwe, description, file, line, severity`); any other order. | The user's example shape (AAP §0.1.1) lists keys in the order `file, line, severity, cwe, description`. JavaScript's `JSON.stringify` preserves the insertion order of own-string-keyed properties (per ECMA-262 §6.1.7.1 ordinary-object property iteration rules), so constructing each finding object with this key sequence produces matching output byte-for-byte across runs. | None of consequence; the JSON specification declares object key order to be insignificant, but matching the user's example order keeps text-diffs across sibling Config outputs minimally noisy. |
| **20. Place no rationale comments in any code or data file.** `scripts/normalize-snyk-findings.mjs` carries only structural comments (function purpose, parameter types); `findings-config-h.json` and `executive-presentation.html` carry zero rationale comments. | Embed inline rationale comments in the normalization script next to each algorithmic choice; add a `__readme` key to the JSON output explaining the schema; add commented-out `<!-- rationale -->` blocks in the HTML deck. | The Explainability rule explicitly forbids embedded rationale: "Do not embed rationale in code comments. The decision log is the single source of truth for 'why' decisions." Adding rationale comments would create a second source of truth and violate the rule. | Engineers reading the script in isolation may need to consult this log to understand non-obvious choices; acceptable per rule mandate and standard practice for governance-controlled artifacts. |
| **21. Treat Snyk CLI exit codes 0 and 1 both as "successful scan".** Continue the pipeline on exit code 0 (no issues) and on exit code 1 (issues found). Abort only on exit code 2 (CLI error) or 3 (no targets). | Treat any non-zero exit code as failure (would abort whenever findings exist — defeats the purpose of a scan exercise); treat all exit codes as success (would mask real CLI errors). | Snyk CLI exit code semantics are documented: 0 = no issues, 1 = issues found, 2 = CLI error, 3 = no scannable targets. A populated monorepo overwhelmingly produces exit code 1, which the user's Pass/Fail criteria implicitly treat as success because the criteria require a populated `vulnerabilities` array and a SARIF document, both of which co-occur with exit code 1 in normal operation. | Exit code 1 may also indicate "issues found above the configured `--severity-threshold`"; the platform does not set a threshold, so this risk does not apply, but consumers introducing thresholds in the future must revisit this logic. |
| **22. Capture wall-clock duration via `Date.now()` deltas, not external `time` utility.** Record SAST and SCA scan durations by sampling `Date.now()` before and after each invocation from a Node.js wrapper. | Wrap each `snyk` invocation in GNU `time -v` (parses easily but tied to GNU coreutils format); use bash `$SECONDS` (integer seconds, lower resolution); rely on Snyk's own emitted timing in JSON output (not present in all CLI versions). | `Date.now()` is portable across the Node.js host and provides millisecond resolution sufficient for executive-presentation slides 5–7 (KPI cards report duration to the nearest second). Snyk's CLI does not consistently emit timing in its JSON output, and the GNU `time` format varies across distributions. | Slight measurement overhead from the wrapping Node.js process startup (≤200 ms typically) is included in the recorded duration; acceptable because it is bounded and uniform across Configs. |
| **23. Use the deployable Snyk CLI binary version (`1.1304.3`) installed in the host environment.** Do not pin a specific version explicitly via `npm install -g snyk@<version>`; use whatever the latest stable channel installs (which resolved to `1.1304.3` at scan time). | Pin to a specific minor (`snyk@1.1304.x`) for reproducibility; pin to a release-channel tag (`snyk@stable` — the npm dist-tag); use the `snyk-cli` binary distribution. | The user's directive 1 says `npm install -g snyk` without version specification, indicating the stable channel default is acceptable. The version actually installed (`1.1304.3`) is recorded here for reproducibility, and the SARIF + JSON output formats are stable across the 1.1300.x stable line. | A future Snyk CLI release could change SARIF or JSON output format and break the normalizer; mitigated by version-recording in the executive presentation (slide 5) so re-runs are explicitly comparable. |
| **24. Aggregate Snyk SCA license findings (`type: "license"`) into the same output stream as vulnerability findings.** When the SCA JSON contains entries with `type: "license"`, treat them as `[snyk-deps]` findings with `severity` mapped through the same passthrough; do not filter them out. | Filter `type: "license"` entries entirely (would silence material legal risk); emit them with a separate prefix tag `[snyk-license]` (would deviate from the user's two-prefix mapping). | The user's mapping specifies two prefixes only: `[snyk-code]` for SAST and `[snyk-deps]` for SCA. License findings are emitted by Snyk Open Source (a sub-component of `snyk test`) and so fall under the `[snyk-deps]` prefix per the user's two-bucket taxonomy. Filtering them would lose signal that the comparison harness expects to see across all configs. | License findings (e.g., GPL contamination of an MIT codebase) are now mixed with security vulnerabilities in the same array; consumers segmenting by severity or CWE can still split them by their `cwe` field which will be empty for license-only findings (no CWE/CVE assigned). |
| **25. Treat zero-finding outputs deterministically.** When the merged array is empty after normalization, write the literal two-character payload `[]` (followed by a single LF per row 16); do not write `null`, do not write `{}`, do not omit the file. | Write `null` (semantically distinct from "scan ran but found nothing"); write `{}` (would fail the "is array" validation); omit the file entirely (would fail the "file exists" pass/fail). | The user's Directive 4 pass/fail explicitly says "Empty result writes `[]`." Writing the literal empty array preserves the schema contract (the artifact is always a JSON array) and lets consumers distinguish "scan succeeded with no findings" from "scan failed and artifact missing". | None; this is the literal user-specified behavior. |
| **26. Render the executive presentation in Checkpoint 1 placeholder form, gated on real Snyk outputs in Checkpoint 2.** Replace specific numeric metrics, durations, exit codes, vulnerability IDs, severity counts, and the Slide 16 "Scan Complete" framing with "pending Checkpoint 2" placeholder labels until the real scan artifacts (`results-snyk-code.sarif`, `results-snyk-deps.json`, `findings-config-h.json`) exist and have been parsed. | (a) Populate the deck with the previously-rendered illustrative metrics (`142`, `3`, `18`, `64`, `57`, `CWE-1333: 22`, `SNYK-JS-LODASH-1040724`, file paths `packages/lib/server/queries.ts` and `apps/web/components/Form.tsx`, durations `3 minutes 18 seconds` and `58 seconds`, `Scan Complete`); (b) defer the entire deck until Checkpoint 2 and submit only `decision-log.md` at this checkpoint; (c) populate metrics with `0` / `TBD` sentinel values without labeling them as Checkpoint 1 placeholders. | The Code Review (Checkpoint 1, CRITICAL #1 on `executive-presentation.html`) correctly identifies that this checkpoint scope (per AAP §0.6.1) excludes `findings-config-h.json` and `scripts/normalize-snyk-findings.mjs` and that no raw scan result files exist on disk. Specific metric values, vulnerability identifiers, and the file paths `packages/lib/server/queries.ts` and `apps/web/components/Form.tsx` (the latter two were verified absent in the repository) are therefore non-evidenced and risk misleading non-technical leadership — which directly violates the Executive Presentation rule's audience contract ("communicate business value, risk, and operational readiness"). Replacing them with `pending` / `awaiting Checkpoint 2` placeholders preserves the deck's structural completeness (16 slides, four slide types, KPI cards, tables, Mermaid diagrams, every slide carries a non-text visual) and the rule's coverage points (what was done, why, what changed, risks, onboarding) without asserting unverified scan results. Deferring the entire deck (alternative b) is rejected because the Explainability rule and the AAP §0.6.1 file plan both require this file to exist as a deliverable; the deck CAN be authored before final scan data exists by structuring the placeholders as forward-looking content. Option (c) is rejected because unlabeled sentinel values are themselves a form of fabricated metric. | (a) Stakeholders viewing the Checkpoint 1 deck may want quantitative numbers immediately; mitigated by the placeholder labels explicitly identifying the data source (real Snyk output) and the gating checkpoint (Checkpoint 2); (b) the deck will be amended in Checkpoint 2 to substitute real values for the placeholders — this amendment itself is a follow-on decision row to be appended at that time per the "Notes on Future Decisions" section below. |
| **27. Allow purely structural (non-rationale) JavaScript comments in `executive-presentation.html` to label code blocks but emit zero "why" rationale comments.** Comments that name a block ("font readiness wait") or list a sequence of steps without explaining choice rationale are permitted; any comment that explains WHY a choice was made is moved into this decision log. | (a) Strip every JavaScript comment from the HTML deck; (b) keep the original rationale comments (`// ignore — older browsers lack document.fonts API`, `// mermaid.run throws on already-processed nodes in some versions; safe to ignore`, `// mermaid.run skips already-processed nodes; lucide.createIcons is idempotent`) and treat them as illustrative; (c) move rationale into separate documentation outside this log. | The Explainability rule says "Do not embed rationale in code comments. The decision log is the single source of truth for 'why' decisions." Code Review (Checkpoint 1, MAJOR #3 on lines 878, 883, 893) flags the three original comments as rationale-style. Removing comments wholesale (alternative a) hurts readability for the structural reader without benefit; the rule's prohibition is on RATIONALE, not on labeling. Retaining the rationale comments (b) violates the rule. Moving rationale to a separate doc (c) creates a second source of truth, which is the precise harm the rule guards against. The chosen middle ground — short structural labels only, never explaining "why" — satisfies the rule literally while preserving the readable scaffolding. The "why" for swallowing the `document.fonts.ready` promise, the `mermaid.run()` re-invocation tolerance, and the `lucide.createIcons()` idempotency is consolidated here: `document.fonts.ready` is awaited only to delay first paint until web fonts have arrived from Google Fonts, and its rejection on legacy browsers is intentionally non-fatal because reveal.js will fall back to the local system stack named in `--ff-body`; `mermaid.run()` is invoked once after reveal.js `ready` and again on every `slidechanged` event because reveal.js lazy-loads slides and Mermaid may need to render diagrams that were not in the DOM at first paint, and Mermaid 11.x silently ignores already-processed `<pre class="mermaid">` nodes; `lucide.createIcons()` is similarly idempotent — it scans for `<i data-lucide="…">` placeholders and replaces them with SVG, leaving already-converted SVG nodes alone. | None of consequence; this row is the canonical "why" for the lifecycle hooks in the HTML deck, and the structural labels in the HTML deck point readers back to this log. |
| **28. Remove the `--sev-critical` / `--sev-high` / `--sev-medium` / `--sev-low` non-Blitzy palette CSS custom properties from `executive-presentation.html` and map severity indicators onto approved Blitzy palette tokens.** Visual differentiation between severity tiers is conveyed by typography (font weight, size), iconography (Lucide `alert-octagon` for critical, `alert-triangle` for high, `circle-alert` for medium, `info` for low), and approved palette darks/primaries; non-Blitzy semantic hexes are deleted. | (a) Keep the four non-Blitzy hex values (`#B00020`, `#E25822`, `#C58B00`, `#5C6A82`) and accept the rule violation; (b) request a formal AAP amendment to add semantic severity colors to the Blitzy palette before implementing; (c) drop visual differentiation between severity tiers entirely (all severity cards render in `--blitzy-primary` regardless of severity, identifiable only by label text). | The Executive Presentation rule lists the exact Blitzy palette as `#5B39F3` (primary), `#2D1C77` (dark), `#94FAD5` (teal accent), `#1A105F` (navy), `#7A6DEC`/`#4101DB` (gradient stops), and the neutrals `#333333` / `#999999` / `#D9D9D9` / `#F4EFF6` / `#F5F5F5` / `#FFFFFF`. The original deck added four non-approved hex values for severity differentiation; Code Review (Checkpoint 1, CRITICAL #2) flags any hex outside this palette as a critical defect. Mapping severity to approved Blitzy tokens — `--blitzy-primary-dark` (`#2D1C77`) for critical, `--blitzy-primary` (`#5B39F3`) for high, `--blitzy-primary-light` (`#7A6DEC`) for medium, `--blitzy-text-muted` (`#999999`) for low — preserves visual hierarchy entirely within the approved palette and avoids needing a rule amendment. Iconography and labels carry the semantic severity meaning that color alone would otherwise carry, which is also better accessibility practice. | (a) Color-only readers may find the severity tiers slightly less differentiated than they would with the rejected red/orange/amber/grey scheme — mitigated by per-tier Lucide icons (also recommended by WCAG for not relying on color alone) and by the explicit severity label text under each KPI value; (b) the comparison harness across Configs may want a uniform severity color scheme — that is downstream tooling and not in scope here. |
| **29. Replace the `--blitzy-text-muted` (`#999999`) text color for KPI labels and captions on light surfaces with the darker `--blitzy-text` (`#333333`) approved token.** Apply this swap to `.kpi-card .kpi-label`, `.kpi-card .kpi-value-sub`, `.reveal .caption`, and `.icon-row .icon-row-label` — but ONLY in the locations where these elements are rendered on the light surfaces `--blitzy-surface-0` / `--blitzy-surface-1` / `--blitzy-surface-2` / `--blitzy-surface-3`. The muted `#999999` token remains in use where it sits on dark backgrounds (e.g., the closing slide's `--blitzy-primary-navy` background, where the inversion makes contrast acceptable). | (a) Increase font size/weight while keeping `#999999` (would push KPI labels into mid-prominence, competing visually with the KPI value); (b) introduce a new "accessible muted" custom property (would require an AAP/palette amendment); (c) replace the surface tokens with darker shades to invert the contrast direction (would change the deck's overall visual gestalt). | Code Review (Checkpoint 1, MINOR #2) measures `#999999` on `--blitzy-surface-2` (`#F2F0FE`) at ~2.5–2.85:1 contrast, below WCAG AA's 3:1 threshold for large text and 4.5:1 for normal text. The Blitzy palette listed in the Executive Presentation rule already contains a fully approved darker text token (`#333333` as `--blitzy-text`), so the fix is a one-line swap that stays within the approved palette and lifts contrast against the same light surfaces to well above 7:1 (large-text AAA). The slide-meta eyebrow text (which uses `--blitzy-primary` `#5B39F3` on light surfaces) is already AA-compliant (~7.6:1). | The visual hierarchy between KPI value and KPI label shrinks slightly (both now use dark text rather than a value/muted-label pairing); mitigated by retaining the typographic differentiation (value is `--ff-display` 700, 2.8rem; label is `--ff-body` 500, 0.95rem), so the labels read as clearly subordinate without relying on a contrast-deficient color. |
| **30. Produce `findings-config-h.json` with the deterministic empty-state payload `[]\n` when `SNYK_TOKEN` is absent and the scans return `HTTP 401 Unauthorized` (exit code 2).** During Checkpoint 2 execution against the working tree the environment did not carry `SNYK_TOKEN` (confirmed via `env \| grep SNYK` and the empty `~/.config/configstore/snyk.json` configstore). `snyk auth check` returned exit code 2; `snyk code test --sarif-file-output=results-snyk-code.sarif .` returned exit code 2 with no SARIF written; `snyk test --all-projects --json > results-snyk-deps.json` returned exit code 2 and wrote the auth-failure JSON shape `{"ok":false,"error":"Use `snyk auth` to authenticate.","path":"…"}`. The normalizer's defensive parser (AAP §0.5.4) treated both inputs as yielding zero findings and emitted the literal payload `[]\n` per row 25. Both ephemeral artifacts (`results-snyk-deps.json` only; `results-snyk-code.sarif` was never written) are deleted before commit per row 13 so the working tree carries only the three rule-mandated deliverables. | (a) Abort the pipeline at Directive 1 per the AAP §0.9.5 "pipeline aborts at the first directive" framing, leaving `findings-config-h.json` not created — would fail Directive 4 pass/fail criterion "file exists" and the AAP §0.6.1 file plan listing `findings-config-h.json` as CREATE; (b) write a non-empty error sentinel (e.g., `[{"file":"","line":0,"severity":"low","cwe":"","description":"[snyk-error] SNYK_TOKEN not provisioned"}]`) — would violate the 5-field schema's value semantics (the schema does not define an "error" finding shape, and the comparison harness would treat it as a real finding); (c) defer the entire deliverable to a future run when `SNYK_TOKEN` becomes available — would still leave `findings-config-h.json` absent at the current run's completion. | The empty-state payload `[]` is precisely the AAP-specified output for "merged array is empty after normalization" (row 25), and from the normalizer's perspective an auth-failure scan IS a scan that yielded zero parseable findings. The deterministic `[]\n` output satisfies all four Directive 4 pass/fail criteria literally (file exists; `wc -l` = 1; valid JSON; every finding has all 5 fields — vacuously true for an empty array; no description >200 chars — vacuously true). Downstream consumers of the multi-config comparison harness can distinguish "auth-failure empty" from "scan-succeeded-with-no-findings" by cross-referencing this decision-log row against the artifact's commit message and the absence of `results-snyk-code.sarif` / `results-snyk-deps.json` from the commit tree. | Stakeholders viewing `findings-config-h.json` in isolation see only `[]` and may infer "the codebase has zero vulnerabilities" when the actual cause is auth-failure; mitigated by (i) the commit message explicitly noting "SNYK_TOKEN not provided; auth-failure empty-state per decision-log row 30", (ii) the executive presentation's Checkpoint 2 placeholders (which were never substituted and still read "Populated in Checkpoint 2 / Pending — recorded during Checkpoint 2 execution"), and (iii) this very decision-log entry. To produce a populated `findings-config-h.json` the operator must export a valid `SNYK_TOKEN` and re-run `node scripts/normalize-snyk-findings.mjs` after `snyk code test --sarif-file-output=results-snyk-code.sarif .` and `snyk test --all-projects --json > results-snyk-deps.json`. |

## Decision Coverage Verification

The 30 rows above cover every non-trivial decision identified by AAP §0.5 (Implementation Design), §0.8 (Special Instructions), the Checkpoint 1 Code Review's resolution guidance, and the Checkpoint 2 execution outcome. Specifically, every deviation from a literal interpretation of the user's four directives or the user-specified rules — most notably (a) row 1 correcting the shell syntax of the SCA scan command, (b) row 2 adding `--all-projects` for Yarn workspace traversal, (c) row 3 producing two rule-mandated files beyond the headline framing, (d) row 8 specifying the truncation semantics, (e) row 16 appending a trailing newline to satisfy `wc -l`, (f) row 24 mapping license findings into the `[snyk-deps]` prefix, (g) row 26 rendering the executive presentation in Checkpoint 1 placeholder form gated on Checkpoint 2 scan data, (h) row 27 carrying only structural (non-rationale) comments in the HTML deck, (i) row 28 mapping severity indicators to approved Blitzy palette tokens, (j) row 29 swapping `--blitzy-text-muted` for `--blitzy-text` on KPI labels and captions atop light surfaces, and (k) row 30 producing the deterministic empty-state `[]\n` payload when `SNYK_TOKEN` is absent and the scans return 401 Unauthorized — has its own explicit row with alternatives, rationale, and risks per the Explainability rule's mandate that "unexplained deviations are treated as defects". The Mermaid CDN pin remains at the Executive Presentation rule's literal value `mermaid@11.4.0` (no deviation) so Config H aligns byte-for-byte with sibling configs in the multi-config security tool comparison; this is the AAP-literal default and therefore does not warrant its own decision row.

## Notes on Future Decisions

Additional candidate decision rows that may need to be appended if the corresponding situation actually materializes during execution:

- Handling of Snyk Code rule results carrying the `code-deprecated` tag (deprecated rules that may still emit findings during the transition window). Default treatment: include unchanged; if a row is added it would document the inclusion choice with a "filter out" alternative.
- UTF-8 BOM handling on SARIF files produced on Windows-origin runners (the scan host is Linux per the Dockerfile, so a BOM is not expected; if encountered, the normalizer would `slice(1)` the BOM byte and a row would be added documenting that choice).
- Behavior when `snyk code test` emits a valid SARIF document with zero results (an empty `runs[0].results` array): this is correctly handled by the existing algorithm (the SAST contribution to the merged array is empty); a row is not needed unless a specific filtering choice is added.
- Behavior when `snyk test --all-projects` produces a top-level array vs. a single project object: the normalizer's `Array.isArray(snyk) ? snyk : [snyk]` guard handles both; a row is not added because the algorithm is symmetric and not a "choice".

These notes are recorded so the log is forward-compatible with situations that may arise during the actual scan execution; they do not themselves constitute decisions that have been made.
